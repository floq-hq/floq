# Floq ML — pinned deps for the export pipeline.
#
# IMPORTANT: use a Python 3.11 or 3.12 venv. PyTorch/TensorFlow do not yet ship
# stable wheels for 3.13/3.14. The system python here is 3.14 — create an
# isolated env first:
#
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r ml/requirements.txt
#
# onnx2tf pulls a large transitive tree (tensorflow, onnxruntime, onnx-graphsurgeon,
# etc.). If pip resolution fights you, follow the version matrix in the onnx2tf
# README — these top-level pins are a known-good starting point.

torch==2.4.1
onnx==1.16.2
onnxruntime==1.19.2
tensorflow==2.17.0
onnx2tf==1.26.3
numpy==1.26.4
