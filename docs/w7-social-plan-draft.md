# Floq W7 Social Plan — adversarial-workflow draft (v14)

> Generated 2026-06-01 by the `w7-social-iterative-plan` workflow: 2 seed drafts (Ship&Learn vs Billion-Ceiling) merged, then 14 rounds of dual adversarial critique (rotating lenses: ship / ceiling / red-team / user / completeness / pragmatist) + integration. Stopped manually at v14 (still materially changing — never hit the 3-stable-round convergence). Critics read the actual codebase; findings reference real files. **This is a machine-generated draft for Mohamed to review, not a locked spec.**

---

title: Floq W7 — The Partnership Core (Phase A). RECRUIT-FIRST, SHIP & LEARN. (v14 — two VERIFIED internal contradictions and a VERIFIED release-blocker force a material re-cut. (1) Critic A-1 lands the decisive completeness kill, VERIFIED: `updateDisplayName` (userProfile.ts:64) writes `users/{uid}.display_name` at ANY TIME with no re-projection hook, while M7.2 only writes the name projection on the FINALIZE path — so (a) a dormant friend with zero sessions has NO projected name, meaning v13's celebrated headline fix ("{name} joined — hasn't focused yet") RENDERS BLANK, a direct F1-states-h × M7.2 contradiction, and (b) a post-pair display-name edit to a slur/phone-number poisons the partner-visible name with no gate (format-strip is not content-check). ADOPTED: DECOUPLE the projection from finalize — write it on pair-ACCEPT (so the dormant fix has a name) and on display-name EDIT (so it can't go stale/poison), add a content check at the projection boundary, enumerate the projection writer as named M-work. (2) Critic B lands the release-blocker, VERIFIED against firestore.rules:24-26: the partner-read predicate `isPartner()` requires reconstructing sorted-UID `pairId` and a cross-doc `get()` into `partnerships`, and the `acceptInvite` partnership-CREATE write-rule needs 4+ `get()`s (both pointers + invite) against the Spark 10-`get()` cap — and v13 test-pinned ONLY the singleton-pointer transaction while DEFERRING the get()-count to W9. You CANNOT defer the get()-budget of the rule that gates your headline write: if it exceeds the cap, EVERY `acceptInvite` fails and the entire Axis-A read is ZERO. ADOPTED: pull the create-rule get()-budget AND the sorted-UID partner-read predicate onto Day 1-2 as release gates with their OWN emulator tests, BEFORE any UI builds on them — exactly the unreviewed code in the Egypt window. (3) Critic B VERIFIED: `deleteSubcollection` is typed `'sessions'|'tasks'` (a literal union, single-docs unhandled) AND flipping the co-owned partnership + clearing the EX-partner's `users/{otherUid}/partner` pointer requires a NEW write-grant to another user's tree that the owner-only rule (lines 22/25) currently FORBIDS — a correctness hole in the "never-cut" floor, unreviewed. ADOPTED: name the parallel single-doc deleter + the scoped cross-tree unpair write-grant (a narrow rule: a member may flip THEIR partnership to `ended` and null the counterpart pointer) as explicit M7.lifecycle work with its own emulator test. Also ADOPTED: offline-claim state (F1-states-j), notify-cap OUTSIDE the transaction + idempotency key (Critic A-2/A-8), presence sign-out/just_finished decay + the write-vs-read block clarification (Critic A-3), broadcast single-use-per-claim-MULTI-issue + per-issuer outstanding-edge cap (Critic A-4), the recruiting-funnel instrument ask→yes→sent→installed (Critic A-5), the old-client×new-rules OTA window check (Critic A-6/B-5), F6 self/double/mutual-react enumeration (Critic A-7), F0 write-idempotency key (Critic A-8), the Days-2-3 serialization honesty (Critic B-3), and the `floq://`-is-100%-typed-code framing correction (Critic B-6). REJECTED: Critic B's route-rename concern — VERIFIED the route is ALREADY `partner.tsx` (no `friends.tsx` rename in the OTA), so that sub-point is moot; the substantive new-store-hydration OTA point is kept. Carries forward v13's verified closes: the notif fix as a ~10-line generalization of the EXISTING two-site cancel-on-entry pattern (useStartSession.ts:43 + RestoreSessionPrompt.tsx:52, both S's lane — Critic A-1 v13), recruit-from-Day-0 (the numerator), broadcast-card as first-class supply, the inviter-oriented dormant state (per mobile/CLAUDE.md:102 "waiting on {name}"), the ONE consolidated consent, the invite's second calm home, the in-app finish card, context-anchored reactions, title-stripping projection, staleness TTL, the slip-not-revert tripwire, and the "underpowered → UNANSWERED, do not launder" terminal — KEPT VERBATIM.)

## 1. Thesis (2 sentences)
W7 ships the smallest social system that produces ONE clean W8 read on the dominant, design-addressable multiplier this two-person team can actually measure — Axis A: does the friend-pairing LOOP FIRE and COMPOUND — RECRUIT-FIRST (a named M-owned list worked from Day 0 in parallel with code, its OWN ask→yes→sent→installed funnel instrumented, PLUS the broadcast-card claim as a first-class organic-supply mechanic), because the verified cause of death is the numerator, not a perfect meter. The privacy/erase floor and the consent surface are CORRECT-AND-SHIPPED but the two rule predicates the read actually transits — the `acceptInvite` CREATE-gate's get()-budget (Spark 10-cap) and the sorted-UID `isPartner()` partner-read — are pulled onto Day 1-2 as release gates with their own emulator tests (if either is wrong the read returns ZERO, and they are the unreviewed code in M's Egypt window), the display-name projection is DECOUPLED from finalize and fires on pair-accept + on edit with a content check (or v13's dormant-state headline fix renders blank), the wipe path's cross-tree unpair write-grant is named as scoped M-work, and Axis B (causal per-pair retention) is honestly DEFERRED to W9.

---

## 2. Feature list — SHIP / SCAFFOLD-DARK / DROP

### SHIP
| # | Feature | WHY | Acceptance criterion |
|---|---|---|---|
| **F-recruit** | **Pair-supply as the M-OWNED SPINE, WORKED FROM DAY 0 — the NUMERATOR, with its OWN top-of-funnel instrument (Critic A-2 v13 + Critic A-5)** | Lift ≈ (pairing rate) × (1 − coupling drag) × (async strength); pairing rate is the one design-addressable multiplier and the ONLY thing that kills the W8 read. | A NAMED recruit list M owns; recruiting STARTS Day 0 and runs in parallel with code EVERY day. **NEW (Critic A-5): the recruiting funnel ITSELF is instrumented — a four-column tally (asked / said-yes / code-sent / installed-and-paired) kept in a sheet or MMKV, logged daily.** Without the ask→install denominator a Day-5 "thin pairs" reading cannot distinguish "the loop is broken" from "M didn't ask enough people" — and the §7 "Low Axis A → build the Room" branch hinges on exactly that distinction. Sized for Axis-A loop-readability (~10-16 pairs), NOT a powered floor. NO RANDOMIZED ARM (Axis B DEFERRED to W9). Day-5 TRIPWIRE (KEEP VERBATIM): too thin → BETA SLIPS A WEEK, recruiting continues, NEVER fires L18 — the LAST resort, made unlikely by Day-0 recruiting. |
| **F-broadcast-supply** | **Broadcast-card claim as a first-class organic-supply mechanic — WITH a bounded abuse ceiling (Critic A-4)** | The ONLY mechanism that makes NEW pairs WITHOUT M hand-recruiting each one. | The reward card shared to a group chat carries a claimable code (F1); a claim lands a COARSE-ONLY consented edge, issuer-notified (spam-capped) + bulk-revocable. **ABUSE CEILING RESOLVED (Critic A-4): the broadcast code is MULTI-CLAIM (so a group chat works — single-use would make all-but-one claimer fail in the exact channel we lean on) BUT carries a hard PER-CODE OUTSTANDING-EDGE CAP (e.g. ≤5 coarse edges per code) and a SHORT TTL — so one card in a 200-person Discord cannot fan out unbounded coarse presence. Cap is PREVENTION (enforced at claim-time in the write rule); bulk-revoke remains the CLEANUP. This distinguishes "rate-limited" from "open firehose with a delete button" for App-Store 1.2.** Per-1:1-invite codes (F1) stay single-use. `claimed_via_broadcast` tagged; claim funnel first-class. Coarse-only / consent-gated / revocable / reportable; no marketplace, no L4/L2 break. |
| **F0** | **`analytics_events` sink + meta-instrumentation — HONEST scope, WRITE-IDEMPOTENT (KEPT + Critic A-8)** | VERIFIED: no analytics package; Firebase Analytics would add a colliding native module. | Append-only create-only `analytics_events/{eventId}`, rules modeled on the VERIFIED `training_samples` create-only/`hasOnly` precedent (firestore.rules:36). OTA, no native module. Thin `logEvent(name, props)` (enqueue + best-effort flush). Shape `{uid, name, ts, seq, kind, props}`; `props` shape-validated coarse map — NO task titles, free text, or display name (L4 at rules). **WRITE-IDEMPOTENCY (Critic A-8): the eventId is a deterministic client key (`uid:seq` or a content hash), so a flush-on-reconnect that partially fails and re-enqueues OVERWRITES rather than DOUBLE-WRITES — an append-only sink with retry and no idempotency key would inflate every convert/react/share rate §7 reads. No TTL/cap needed at beta volume, but the dedup key is required.** HONEST DETECTOR SCOPE: the `seq`-gap detector is an OFFLINE W8 Admin-SDK analysis, churn-blind — §7 treats absence as ambiguous. Day-1 acceptance: rules prove create-only + no-free-text + no-display-name; a smoke event lands; S fires `logEvent` with no rebuild. No `arm` tag. DEMOTED from gold-plated. |
| **F0-nonengage** | **ONE non-engagement counter: ignition-seen-not-acted (KEPT)** | The felt failures that matter are non-events. | One cheap counter via F0: ignition-surface-SEEN-but-not-acted, MMKV-synchronous on render; an `AppState` background transition with the flag set + no session started fires the counter and clears the flag. DEFERRED to W9: the other two counters. |
| **F1** | **Invite-code install-and-pair — friend LANDS ALREADY PAIRED + no-friend pass-through + COARSE-ONLY broadcast edge + one-partner SINGLETON POINTER + the CREATE-rule get()-budget as a RELEASE GATE (KEPT + Critic B-2)** | Pairing rate is the dominant multiplier and the core of the Axis-A read. | **Code path:** invite card carries a 6-char code (printed + embedded in link). Cold install → onboarding → auth → single typeable "Have an invite code?" field → atomic `acceptInvite(code)` → partnership lands `active` immediately. **`floq://`-IS-100%-TYPED-CODE (Critic B-6, framing fix): a `floq://` scheme link CANNOT open an uninstalled app, so at beta n the link NEVER survives a cold install — pairing is carried 100% by the typed 6-char code. The field is the PRIMARY path, the link is a best-effort pre-fill only. Convert-rate instrumentation attributes NO pairing to the link; the recruiting script is "text them the CODE + a store link," not "send a link that pairs them."** **ONE-PARTNER ATOMICITY: `users/{uid}/partner` singleton pointer; `acceptInvite` `get()`s BOTH pointers + the invite in the transaction and rejects if either is pointed. CREATE-RULE GET()-BUDGET IS A RELEASE GATE (Critic B-2, NOT a W9 deferral): the partnership-CREATE rule must `get()` both pointers + the invite (≈3-4 `get()`s) against the Spark 10-`get()`/rule cap; this count is enumerated and proven under-cap by a Day-1/Day-2 emulator test — because if it exceeds the cap EVERY `acceptInvite` fails and the entire Axis-A read is zero. This is distinct from the snapshot-read get()-cost (genuinely W9-deferred).** **REVIEW MITIGATION (Critic A-3 v13 + Critic B-1): no second backend reviewer, so BOTH the singleton-pointer transaction (second-partner rejection + simultaneous-claim idempotency) AND the sorted-UID `isPartner()` partner-read predicate are pinned by Day-1/Day-2 emulator tests S can run and read.** No-friend pass-through: one-tap "Skip — focus solo" (return path F1-skip). Entropy: 6 chars / 32-symbol, ≤72h, server-collision-checked, revocable. Claim idempotent on `pairId` (sorted-UID). Every funnel step fires `logEvent`. |
| **F1-states** | **Invite/claim edge-case states — incl. the INVITER-ORIENTED dormant state AND the OFFLINE-CLAIM state (Critic B-1 v13 + Critic A-2)** | At small n everyone invites everyone; unhandled branches corrupt the read AND own the worst FELT moments. | Each branch a defined outcome + failure-reason code (F0): (a) second incoming invite while paired → rejected via singleton pointer; (b) mutual/simultaneous → idempotent on sorted-UID `pairId`; (c) expired-during-install-gap → "ask for a new one"; (d) self-pairing → rejected; (e) revoked code → explicit revoked state; (f) re-pair SAME two UIDs after `ended` → NEW claim re-activates ONLY IF neither `blocked_by`; resurrected edge starts FRESH (`session_epoch`++); (g) re-pair where target previously BLOCKED claimer → rejected; (h) **PARTNER-NO-SHOW (`partner_dormant`): zero completed sessions since pairing. FELT FIX (per mobile/CLAUDE.md:102): the inviter sees a quiet, NON-BLAMING "{name} joined — hasn't focused yet" + a resend/nudge affordance; the friend sees their own normal solo experience; `partner_dormant` tagged in F0. NAME DEPENDENCY RESOLVED (Critic A-1): the `{name}` rendered here comes from the projection written ON PAIR-ACCEPT (F2-name, decoupled from finalize) — NOT from a finalized session the dormant friend never had, so this state no longer renders blank;** (i) DEAD ISSUER → explicit "this invite is no longer valid"; **(j) OFFLINE / NO-CONNECTIVITY AT THE INSTALL→AUTH→PAIR SEAM (Critic A-2, NEW): `acceptInvite` is a transaction and the most common real-world failure is the network (airplane/subway/fresh-cellular). The friend who just installed and gets a spinner-then-nothing is the activated evangelist's friend — failing silently is the worst outcome. RESTORED: an explicit offline state ("Couldn't reach Floq — check your connection and tap Retry"), the typed code persisted locally so Retry re-attempts without re-typing, and a clear non-paired-yet status until the transaction commits.** NO `pending_issuer_ack`/`ackPairing`/`declinePairing`. |
| **F1-skip** | **No-friend "Skip — focus solo" + a non-inert "want a partner" intent surface (KEPT)** | The partnerless user is the entire reason the Room exists; a polite cul-de-sac is a real felt cost. | "Skip — focus solo" lands calmly in solo. The Partner tab for a no-partner user carries an "I want a partner" intent toggle that WRITES a queued intent (the settled "never inert" intent) — banked for W9 Room/match seeding, surfaced as a calm "we'll let you know when there's someone to pair with." Does nothing live in W7 (Room dark) but is NOT a cul-de-sac; Skip-then-return rate is a NAMED §7 health guard. Solo NEVER blocked. |
| **F2** | **1:1 partner edge + partner-visible session summaries — via title-stripping `social/summary` (VERIFIED LEAK, KEPT)** | The retention hook and the moat. VERIFIED `toSessionDoc` writes `title: s.task.title` at session/distraction.ts:42. | Partner-readable surface is a SEPARATE projected doc `users/{uid}/social/summary` written by a projection writer copying ONLY {minutes, focus_score, ended_at, phase_at_end}, provably OMITTING `task.title`. Partner READ is an EXACT-PATH rule match `/users/{uid}/social/summary` — NOT `/social/{doc}`, NOT any `=**`, NOT the `{document=**}` owner-grant (lines 24-26). **THE PARTNER-READ PREDICATE IS THE RISKIEST RULE (Critic B-1): `isPartner()` must reconstruct sorted-UID `pairId` from `request.auth.uid` + the resource owner `uid` and cross-doc `get()` into `partnerships/{pairId}` — fiddly and unreviewed, so it gets its OWN Day-1/Day-2 emulator test (partner CAN read; non-partner/removed/blocked DENIED), not just the singleton test.** Rules test proves: partner CAN read `social/summary`; partner DENIED on `users/{uid}/sessions/{id}` (VERIFIED line 42), `tasks/**`, `social/<other>`, AND the raw `users/{uid}` doc (holds `display_name` + email). Data-read opt-in folded into the SINGLE pairing consent (F3a). |
| **F2-name** | **Partner display name as a SHAPE-VALIDATED, CONTENT-CHECKED, MODERABLE, WIPED projection — DECOUPLED FROM FINALIZE, written on PAIR-ACCEPT + on EDIT (Critic A-1, MATERIAL FIX)** | VERIFIED `display_name` is user-typed FREE TEXT (userDoc.ts:18) AND `updateDisplayName` (userProfile.ts:64) edits it at ANY time; read directly by a partner it bypasses every guard. | The partner-visible name is a DEDICATED projected field on `social/profile` (sibling of `social/summary`), NOT the owner doc. **PROJECTION DECOUPLED FROM FINALIZE (Critic A-1, the decisive fix): the projection writer fires on (1) PAIR-ACCEPT — so a dormant friend with zero sessions HAS a name for the inviter's "{name} joined" state (resolving the F1-states-h × M7.2 contradiction), and (2) `updateDisplayName` EDIT — so a post-pair rename to a slur/phone-number cannot project unchecked and the projection cannot go stale. The finalize-path write is kept only as a refresh, not the source.** The writer applies a LENGTH-CAP + control-char strip AND a **CONTENT check (not just format): a basic phone/URL/slur heuristic at the projection boundary** — because "CALL 555-0123" passes every format filter; the Report path stays as the reactive backstop. Rules `hasOnly` the social-profile doc's keys. MODERATION: one-tap "Report" on any partner/claimer surface → owner-only report doc; with REMOVE/BLOCK this is the App-Store 1.2 abuse path. F-lifecycle ENUMERATES and deletes the projected name doc. Rules test: partner reads the projected name but NOTHING else on the owner tree; field length-bounded. |
| **F2b** | **Reversible silent "pause this partner" (mute) + listener teardown + AMBIENT-PRESSURE read (KEPT)** | Without a quiet middle, annoyed users mute the whole app; AND mute is the only signal for the ambient-pressure felt cost. | One-tap "Mute partner" stops F3 surfaces, leaves the data edge intact, tears down the partner-presence `onSnapshot`, silently reversible, fires no signal to the partner. AMBIENT-PRESSURE READ: mute rate is read in §7 with TWO candidate causes — "annoyed by spam" AND "stressed by a friend's visible diligence" — a mute spike is NOT auto-read as spam. An optional one-tap "too much?" micro-reason (owner-only) disambiguates. Tested. Logged (F0). |
| **F2c** | **Clean exit — REMOVE (neutral) vs BLOCK (indistinguishable, CONFIRM); presence revocation clarified to a READ-RULE gate (Critic A-3, sharpened)** | A coarse-presence stream to someone you want gone is a stalking primitive — and the broadcast-claim revocation backstop. | TWO exits. REMOVE (common): flips `status:'ended'`, clears the singleton pointer, ex-partner resolves to an HONEST neutral "[name] ended the partnership." BLOCK (abuse): ALSO sets `blocked_by`, resolves the blocked party to the wipe-IDENTICAL "no longer connected." **PRESENCE REVOCATION IS A READ-RULE GATE, NOT A WRITE GATE (Critic A-3, correctness fix): `presence/{uid}` is ONE owner-written doc per user (written on every session start), so it does NOT "stop being written" per-partner. Revocation works by the partner-read RULE denying access once `status:'ended'`/`blocked_by` is set — plus the client listener teardown. v13's "stops writing presence at the write layer" was muddled: the doc keeps being owner-written for OTHER partners; the BLOCKED partner simply loses READ. The emulator test asserts a removed/blocked partner is DENIED the read on next eval.** BLOCK (irreversible) carries the confirm; REMOVE (re-pairable) one-tap. Same-UID block-evasion closed (F1-states-g). Logged (F0). |
| **F-lifecycle** | **WIPE / ACCOUNT-DELETE completeness across ALL new egress — NEVER-CUT — incl. a PARALLEL single-doc deleter + the CROSS-TREE unpair write-grant (KEPT + Critic B-3)** | VERIFIED: `wipeRemoteUserData` (userData.ts:64-73) deletes EXACTLY `sessions`+`tasks` via `deleteSubcollection` typed `'sessions'|'tasks'`. Every W7 egress must survive "Clear history." | Extend wipe + account-deletion to enumerate and delete EVERY W7 egress: (1) `social/summary`; (2) `social/profile` name projection; (3) owner-only sentiment doc; (4) owner-only report doc; (5) `presence/{uid}`; (6) the `users/{uid}/partner` singleton pointer; (7) the want-a-partner intent doc. **IMPLEMENTATION HOLE NAMED (Critic B-3): `deleteSubcollection` is typed `'sessions'|'tasks'` (a literal union) and handles SUBCOLLECTIONS, not single docs — most W7 egress are SINGLE docs. So F-lifecycle ships a PARALLEL single-doc deleter (or widens the path type) — named M-work, not a free reuse.** **CROSS-TREE UNPAIR WRITE-GRANT NAMED (Critic B-3): flipping the CO-OWNED `partnerships` doc to `ended` AND clearing the EX-partner's `users/{otherUid}/partner` pointer requires WRITING to another user's tree, which the current owner-only rule (lines 22/25) FORBIDS. So a NARROW new rule ships: a partnership MEMBER may flip THEIR partnership to `ended` and null the counterpart's singleton pointer — scoped to that exact transition, nothing else. This is a deliberate loosening (CLAUDE.md sign-off item) with its OWN emulator test (a non-member cannot touch either doc).** NO `co_sessions` rows — NAMED as the deliberate §9 moat cost. Acceptance: emulator test proves a wiped user is unreadable by their ex-partner across ALL surfaces INCLUDING the name; the cross-tree grant is member-only; Day-6 PASS-1 gate; never-cut. |
| **F3** | **Honestly-ASYNCHRONOUS partner-presence (Beat 1) — IN-APP, RECENCY-LIVE, STALENESS-BOUNDED incl. SIGN-OUT + just_finished decay, listener-lifecycle-managed (KEPT + Critic A-3)** | Attacks pairing→co-session conversion (a CORE Axis-A metric) AND the dial that turns async-strength. | Recipient side: a dismissible surface rendering ONLY when the partner is CURRENTLY in a live focus session (coarse `state:focusing`) as a quiet "[partner] is in a focus session right now" + OPTIONAL "start one too?" reading as the recipient's OWN prompt. **STALENESS TTL — now covers THREE residual-beacon cases (Critic A-3): (1) crash-during-session — F3 renders ONLY IF `state:focusing` AND `started_at` within the ≤90-min clamp+margin (older reads STALE→idle); (2) SIGN-OUT/account-switch — sign-out sets `presence/{uid}` to `idle` (a signed-out user must NOT read `focusing`/`just_finished` forever); (3) `just_finished` DECAY — `just_finished` is bounded by `ended_at` recency (e.g. a short window), so a partner who finished 3 days ago does NOT read "just finished" indefinitely. v13 bounded only the `focusing` lie; all three are now bounded.** LISTENER LIFECYCLE: `onSnapshot` on the partner's `presence/{uid}` mounts ONCE under `app/(tabs)/_layout` keyed on the active partnership, tears down on mute/block/unpair; denied read swallowed per `if(__DEV__)`. Gated on ping-consent, not-muted/blocked, not-in-session, not `partner_dormant`. Never mid-session. Started-from → `started_via:'partner_ignition'` (DIAGNOSTIC) + `logEvent`. AMBIENT-PRESSURE NAMED: the FEELING is real and not neutralized by the staleness fix; mitigations are opt-in (F3a), recipient's-own-prompt framing, and the F2b mute-reason read; §7 watches mute-rate as the (imperfect) proxy, explicitly NOT mislabeled as spam. PROTECTED minimal co-presence tier. |
| **F3a** | **ONE consolidated pairing-consent — not three micro-decisions (KEPT)** | Three consent toggles at the connection moment contradicts the "immediate partner" thesis. | COLLAPSE the data-read opt-in (F2) and the ping-consent into a SINGLE pairing-consent moment with ONE choice: "Share your focus activity with [partner]?" covering both coarse summaries AND live presence — default OFF, one tap, symmetric, per-partner, toggleable. Mute (F2b) remains the reversible kill-switch. OS notification permission stays separate by necessity (requested at SECOND session-end / first-reaction, NOT at pairing). Pairing without sharing allowed. Grant + asymmetry logged (F0). |
| **F4-inapp** | **Partner-activity finish signal — IN-APP-ONLY card on next Home open (digest CUT) + TIMELY-REACTION mitigation (KEPT)** | The async co-presence reward signal; the in-app surface was already built as the denied-permission fallback. | CUT the local `scheduleNotificationAsync` finish DIGEST entirely. Replaced by the IN-APP "what your partner did" card on next Home open, computed from synced `social/summary`. No OS notification, no timezone reconciliation. Gated on not-muted/blocked, not `partner_dormant`. REACTION-TIMELINESS COST NAMED: a fire/clap reaches the recipient only on their NEXT Home open, detached from the session — a NAMED felt cost (openFlaws); the in-app reaction surface shows the ORIGINAL session context ("you clapped Sara's 70-min session") so a cold arrival re-anchors. The timely-push beat is an explicit W9 spend. PROTECTED tier. |
| **F6** | **One-tap completion reaction (Beat 3, in-app) — rate-capped, wiped-summary-safe, context-anchored, FULLY ENUMERATED (KEPT + Critic A-7)** | The cheap reward-beat closure AND an Axis-A loop-fire metric. | On opening a partner's summary card, a one-tap fire/clap — in-app, never notification-prompted, anchored to the original session context. RATE-CAPPED per partner; a reaction targeting a wiped/ended summary is a no-op. **EDGE-CASES ENUMERATED to F1-states rigor (Critic A-7): (a) SELF-react on your own summary — disallowed (no-op); (b) DOUBLE-react (same reaction twice) — idempotent, not double-counted; (c) MUTUAL react (both partners react) — each sees the other's, the per-partner cap is per-direction so the bidirectional case is well-defined; (d) react to a broadcast-claimed stranger BEFORE mutual consent — gated on the same consent/not-blocked predicate as every other partner surface.** Gated on not-muted/blocked. Logged (F0). |
| **F-sentiment** | **One-tap felt-experience instrument — OWNER-ONLY, partner-INVISIBLE, judges the SESSION — PAIRED-CONTEXT TAG KEPT, SOLO-PARITY CUT (KEPT)** | Qualitative color + the cheap `pair_context` tag W9 needs. | A sampled, optional, one-tap post-session micro-prompt: "How was that focus session?" → good/okay/rough. Does NOT name the partner. Owner-only doc; rules test asserts a partner is DENIED reading any sentiment field. SOLO-PARITY MACHINERY CUT (Axis B deferred → no causal arm to protect). PAIRED-CONTEXT TAG KEPT (finalize-path): every finalized session logs `pair_context:'paired'|'solo'` — banked for W9. NO `arm`. Sampled, never blocks reward. The AMBIENT-PRESSURE cost is read via mute-rate (F2b), NOT here. |
| **F7** | **Coarse presence primitive `presence/{uid}` — partner-read PROVEN, minimal WRITE + LIVE staleness-bounded read incl. sign-out/just_finished decay PROTECTED; cost-modeling DEFERRED (KEPT + Critic A-3)** | The single primitive F3 (and the dark Room F8) sits on. | Holds ONLY coarse consented fields — `state:focusing|idle|just_finished`, `phase`, `started_at` (LOAD-BEARING for staleness), `ended_at` (LOAD-BEARING for `just_finished` decay — Critic A-3), optional `score`/`minutes` on finish. Owner-write AND owner-delete. Written `state:focusing` on START, `idle`/`just_finished` on END, **`idle` on SIGN-OUT (Critic A-3)**. Crash case covered by F3's client-side `started_at` gate; sign-out and stale-`just_finished` cases covered by the explicit `idle`-on-sign-out write + the `ended_at` decay. Rules test: active partner can read; non-partner/removed/blocked denied; no title/free-text/name writable (`hasOnly`). DEFERRED to W9: the N≥3 room-member predicate; the SNAPSHOT-read get()-cost assertion (distinct from the release-gating CREATE-rule get()-budget in F1). |

### SCAFFOLD-DARK
| # | Feature | WHY | Acceptance criterion |
|---|---|---|---|
| **F8** | **The Room (ambient co-presence pool) — SCHEMA-ONLY, dark; the zero-signal cost NAMED (KEPT)** | Hedge against low pairing rate AND coupling drag; the sole k>1 / liquidity surface. | `rooms/{roomId}/members` schema ships Day 1 behind `FLAG_ROOM_LIVE=false`. No dark UI render path. DEFERRED off the critical path: density-orchestration spec + the F7 N-member proof + the get()-cost number. Written ONLY if buffer remains. NAMED COST: W7 gathers ZERO empirical Room/liquidity/density signal. The "Low A → build the Room next" branch (§7) rests EXPLICITLY on §9 conviction + the broadcast-card k>1 diagnostic, and the plan SAYS SO. The F1-skip want-a-partner intent doc is the FIRST seed this dark Room will later consume. |

### DROP
| # | Feature | WHY | Acceptance (of the drop) |
|---|---|---|---|
| **F-notif-redesign** | **The "Day-1-full concurrency redesign at focus.tsx's two start sites" (CUT as VERIFIED-FALSE)** | VERIFIED FALSE: focus.tsx:177-196 is a HYDRATION SEAM; the real start sites (useStartSession.ts:43 + RestoreSessionPrompt.tsx:52) ALREADY both `void cancelBreakReminder()` with a "symmetric" comment. | No focus.tsx start-site wiring. No "make startSession async." The fix is M generalizing the service helper to sweep `session-start` (reusing `breakScheduleInFlight` + `cancelByKind`) + S adding it to the two EXISTING cancel sites S owns. The falsely-budgeted Day-1 M-day is RECLAIMED → recruiting. |
| **F4-digest** | **The event-worthy backgrounded local-notification finish DIGEST (CUT)** | Highest-effort-per-unit-of-W8-signal; F4-inapp covers the signal. | No digest scheduling, no `scheduleNotificationAsync` for finishes, no timezone reconciliation, no backgrounded-digest gate. Recovered M-time → recruiting + the privacy floor + the loop. |
| **A7-a — Randomized solo-control arm (CUT)** | **The held-out randomized solo control (CUT)** | OPERATIONALLY IMPOSSIBLE at this n/team. | No arm, no `arm` tag, no powered floor. Axis B DEFERRED to W9. The "paired ≤ solo" causal revert trigger DELETED for W7. |
| **F-coflog runtime writer** | **Same-day co_sessions WRITER (CUT to pure schema)** | Zero W8 read value; costs rules+writer+tz+dedup+lifecycle; re-identifies at this n. | No W7 writer/rule/lifecycle obligation. Banked as PURE W9 SCHEMA. NAMED COST: the graph-asset age-clock does NOT start in W7 (openFlaws). |
| **F-sentiment solo-parity machinery (CUT)** | **Solo-arm parity sampling (CUT)** | Controlled a confound for the deferred Axis-B study. | No solo-parity rate-matching; keep only the one-tap paired prompt + the `pair_context` tag. |
| **F-issuer-ack** | **The `pending_issuer_ack` gate (KEPT-CUT)** | CONTRADICTED "friend lands ALREADY paired." | No `pending_issuer_ack`, no `ackPairing`/`declinePairing`, no issuer-accept UI. |
| **F-push** | **Remote-push native subsystem (KEPT-CUT)** | Every surviving signal ships on local `scheduleNotificationAsync` + in-app. | No `getExpoPushToken`, no APNs key, no token doc. W9 spend (incl. the timely-reaction push). |
| **F-analytics-native** | **Firebase Analytics / any native analytics SDK (KEPT-CUT — VERIFIED)** | Installing it breaks the OTA spine. | Not added. The meter is the hand-rolled OTA `analytics_events` sink (F0). |
| **F5-engine** | **Pair-streak engine + visible streak (DEFERRED to W9; W7 flag-only)** | A visible streak yokes the user to a near-stranger's consistency — coupling drag. | W7 ships only the schema field behind `FLAG_PAIR_STREAK_LIVE=false`. |
| **F9** | **Scheduled-session calendar surface (KEPT-CUT)** | Focusmate's synchronous slot — kills the moat. Supersedes M7.1b (tasks.md line 843). | No calendar UI; `partnership.ts` carries no `scheduled_at`. |
| **F1b** | **Universal-Link auto-paste (KEPT-CUT)** | Consumes a native `associatedDomains` slot; pairing is 100% typed code at beta n (Critic B-6). | No `associatedDomains`; no AASA. W9 nicety. |
| **F-cloudfn** | **Cloud Function push + presence-window log (KEPT-CUT)** | Requires Blaze; L13 + L23 lock Spark-only. | Not shipped W7. W9 escalation. |
| **Live Room for the seed cohort (REJECTED)** | **Lighting a minimal LIVE Room for the 16 recruited friends (REJECTED)** | REJECTED on §4 (16 friends = ghost-town false-negative, T1) + §9. | Not lit. F8 stays schema-only-dark. Zero-Room-signal NAMED. |
| **k-anon aggregate survivor (REJECTED for W7)** | **A wipe-surviving k-anonymized co-occurrence aggregate (REJECTED for W7)** | Adds a writer + k-floor + wipe-survival test on a triple-booked relocating engineer; re-identifies at n~16. | Not written. Banked as W9 schema + k-anon-floor design. No-surviving-structure cost NAMED. |
| **Single-use broadcast code (CUT in favor of multi-claim-with-cap)** | **Single-use enforcement on the BROADCAST code (CUT — Critic A-4)** | Single-use made all-but-one group-chat claimer fail in the exact organic-supply channel; replaced by multi-claim + a hard per-code outstanding-edge cap. | Broadcast code is multi-claim with a hard edge-cap + short TTL (F-broadcast-supply). Per-1:1-invite codes stay single-use. |
| — | **Capacity-driven Tier split (CUT) / monetization (none) / "rules-layer single-use guarantee" (DOWNGRADED to best-effort)** | KEPT-CUT; VERIFIED L11; Spark rules can't bind invite-consume to partnership-create. | A0 forces L11 reconciliation; single-use best-effort + the write-predicate forgery/enumeration close; nothing priced. |

---

## 3. The three beats — concretely

**Beat 1 — Ignition (before), HONESTLY ASYNC, ALIVE, NEVER A PERMANENT LIE — and an ACKNOWLEDGED pressure surface.** On Home/Session, IF you enabled the SINGLE pairing-consent (F3a), are not in session, and the partner is not `partner_dormant`: the surface renders ONLY when the partner is CURRENTLY focusing (live coarse `state:focusing`) AND FRESH (`started_at` within the max-session window). A signed-out partner reads idle (sign-out sets `idle` — Critic A-3); a 3-day-old `just_finished` does NOT render "just finished" (`ended_at` decay — Critic A-3). Quiet "[partner] is in a focus session right now" + OPTIONAL "start one too?" reading as YOUR OWN prompt. No fresh-live partner → silence. HONEST about the felt cost: this IS a social-obligation surface; opt-in, framed as the recipient's own prompt, watched (imperfectly) via mute-rate, NOT mislabeled as spam. Tap → *your own* formula → *your own* timer; tagged `started_via:'partner_ignition'` (DIAGNOSTIC) + `pair_context:'paired'` + `logEvent`. The SENDER sees nothing promising delivery. Seen-but-no-start → the ignition-seen-not-acted counter. No partner → calm pure-solo. **`partner_dormant` → the INVITER sees "{name} joined — hasn't focused yet" + a resend affordance, with `{name}` supplied by the PAIR-ACCEPT projection (Critic A-1) so it is never blank.** The listener mounts once under the tab layout, tears down on mute/block/unpair.

**Beat 2 — Co-presence (during).** Session screen unchanged except a small chip set at *your own* ignition — copy true-at-start, does NOT live-update. Wallpaper, never interruptive. Distraction + backgrounding unchanged (L15). **Zero notifications fire — ENFORCED by generalizing the EXISTING two-site cancel-on-entry pattern. VERIFIED-CORRECT MECHANISM: `useStartSession.ts:43` `launch()` and `RestoreSessionPrompt.tsx:52` `onResume()` ALREADY both `void cancelBreakReminder()` (the latter commented "symmetric with useStartSession.launch"). M adds `cancelEntrySessionReminders()` that sweeps BOTH `break` AND `session-start` (reusing `cancelByKind` + awaiting `breakScheduleInFlight` for audit-#30), and S swaps the two existing calls — ~10 lines at sites S OWNS (mobile/components/session/), NOT a redesign, NOT at focus.tsx (a hydration seam).** On session END the daily `session-start` reminder is RESCHEDULED — ONLY for an enabled+permitted user; the reschedule THREADS the onboarding `PreferredTime` + the enabled-pref; a test asserts the daily reminder still exists with the RIGHT hour after a session across BOTH permission branches (`{request:false}` must NOT silently delete it). The break reminder is swept too. The middle is sacred.

**Beat 3 — Reward (after) — and a SECOND, calmer home for the invite.** On *your* completion the card generates. The invite no longer lives ONLY here: the post-session-drained moment is the user's lowest-energy state, so the reward card stays a shareable artifact (carrying a multi-claim broadcast code with a per-code edge-cap — F-broadcast-supply), BUT the invite ALSO has a calm deliberate home in the Partner tab where a user CHOOSES to recruit. A finish signal reaches your partner ONLY as an IN-APP "what your partner did" card on THEIR next Home open (F4-inapp; digest CUT). On session END the daily reminder is RESCHEDULED for an enabled+permitted user (threaded preferred-time + both-branches test). Opening your summary card → one-tap fire/clap (F6, in-app, rate-capped, context-anchored, self/double/mutual-react enumerated — Critic A-7). OS notification permission requested on the SECOND session-end OR right after the first reaction is RECEIVED — not on the first reward beat, NOT at pairing (F3a keeps pairing to ONE consent). No visible streak (F5 dark). Occasionally a sampled "How was that focus session?" (F-sentiment, owner-only). The broadcast claim notifies the issuer (spam-capped) on first claim; the issuer can BULK-REVOKE.

---

## 4. M/S task split + BUILD-RISK PRIORITY ORDER

**Capacity (A0) + RELOCATION BUFFER:** VERIFIED L11 (decisions.md line 84) locks "normal schedule from Egypt, no gap." Day 0 FORCES a binary reconciliation + amendment (A0). The plan does NOT believe a doc line over a verified physical fact: M is relocating THIS week. 1-2 M-days are EXPLICITLY COSTED against the move; scope cut to fit ~5-6 effective M-days.

**THE HEADLINE THIS ROUND: TWO VERIFIED CONTRADICTIONS + A RELEASE-BLOCKER, all front-loaded.** (1) **The display-name projection is DECOUPLED from finalize (Critic A-1)** — written on pair-ACCEPT (so v13's dormant "{name} joined" fix isn't blank) and on EDIT (so it can't go stale/poison), with a content check. (2) **The `acceptInvite` CREATE-rule get()-budget (Spark 10-cap) and the sorted-UID `isPartner()` partner-read predicate are RELEASE GATES on Day 1-2 (Critic B-1/B-2)** — if either is wrong the Axis-A read returns ZERO, and they are the unreviewed code in M's Egypt window, so each gets its OWN emulator test BEFORE UI builds on it. (3) **F-lifecycle's wipe needs a PARALLEL single-doc deleter + a NARROW cross-tree unpair write-grant (Critic B-3)** — `deleteSubcollection` is typed `'sessions'|'tasks'` and the co-owned partnership flip writes to the ex-partner's tree, which the current owner-only rule forbids; both are NAMED M-work with tests, not free reuse.

**RECRUIT-FIRST, with the recruiting funnel INSTRUMENTED (Critic A-2 v13 + Critic A-5):** recruiting starts Day 0 in parallel with code; the broadcast claim is first-class supply; the privacy/concurrency machinery is correct-and-good-enough not gold-plated; AND the recruiting funnel itself (ask→yes→sent→installed) is tallied so a Day-5 "thin" reading separates "loop broken" from "not enough asks."

**THE DAYS-2-3 SERIALIZATION IS NAMED, NOT HIDDEN (Critic B-3):** v13 listed ~9-10 days of backend (M7.3 + M7.2 + M7.1 + M7.0a + M7.0 + lifecycle) across 2 calendar days with recruiting stacked on top — the arithmetic does not close. RESOLUTION: this is acknowledged as the dominant scheduling risk; the DEFERRABLE pool absorbs it FIRST (F7 N-member proof, get()-snapshot assertion, Room doc, S7.6 all slip before anything in PRIORITY-A), and the Day-5 tripwire SLIPS THE BETA A WEEK if the PRIORITY-A backend is not closeable — the slip is the pressure valve, not optimism. Recruiting being M-owned is a real competitor for those hours and is the explicit reason the slip exists.

**RULES-WORK RE-SIZED:** M7.0a is ~2.5-3 days (4 rule surfaces + `hasOnly` validators on 4 doc types + the cross-tree unpair grant + ~18 emulator cases incl. the two release-gate predicates), Days 1-3, PRE-WRITTEN Day 0.

- **PRIORITY-A (the irreducible Axis-A loop read + privacy/erase floor + the sacred-middle fix — PROTECTED):** F-recruit (Day 0, funnel-instrumented) + the notif cancel-on-entry GENERALIZATION + F0 sink (write-idempotent) + M7.0a scoped rules INCL. the two release-gate predicate tests (Critic B-1/B-2) + the cross-tree unpair grant (Critic B-3) + best-effort single-use + the singleton-pointer guard (test-pinned) + M7.1 `acceptInvite` (offline-state F1-states-j) + F-broadcast-supply (multi-claim + edge-cap) + M7.2 title-stripped summary + the DECOUPLED name projection writer (pair-accept + edit + content-check — Critic A-1) + `pair_context` tag + F-lifecycle wipe-completeness (parallel single-doc deleter — Critic B-3) + S7.0/S7.1 pairing+card + the inviter-oriented dormant state + the Report/moderation surface. F-lifecycle never-cut.
- **PROTECTED CO-PRESENCE MECHANICS (Days 2-3):** MINIMAL F7 presence WRITE (incl. sign-out idle + `ended_at` decay — Critic A-3) + LIVE staleness-bounded single-partner-read + listener-lifecycle + F3 + F4-inapp.
- **DEFERRABLE (first to slip, absorbs the Days-2-3 compression):** F7 N-member proof → get()-snapshot assertion → Room density doc → S7.6 pair-insight UI. F-sentiment + `pair_context` are NOT in the slip pool.

**EXPLICIT DEFAULT READ:** the cleanly-shippable read is the Axis-A PAIRING-RATE + loop-mechanics read; Axis B (per-pair RETENTION) is DEFERRED to W9. Co-presence MECHANICS (F3/F4-inapp/F7) STILL ship. Day-5 tripwire: loop-readable → beta runs; thin → slip a week + keep recruiting.

Per the M/S memory, M does NOT edit `mobile/app`/`mobile/components`. The notif helper is M-owned (`cancelEntrySessionReminders()` in services/notifications); the two call-site swaps are in `useStartSession.ts` + `RestoreSessionPrompt.tsx` — S's lane, sites that ALREADY call `cancelBreakReminder()`. S swaps both; M reviews.

### Mohamed (M7.x) — backend + recruiting (recruiting from Day 0)
- **M7.recruit — recruiting the named F-recruit list, FROM DAY 0, WITH a funnel tally (Critic A-2 v13 + Critic A-5, never-cut, the numerator).** The social ask + code hand-off begins Day 0 against stubs; continues daily. Maintains the ask/yes/sent/installed four-column tally (sheet or MMKV). Sized for loop-readability (~10-16 pairs), NO arm. Day-5 tripwire.
- **M7.notif-fix — GENERALIZE the existing cancel-on-entry helper to sweep `session-start` too (NOT a redesign).** In `mobile/services/notifications/index.ts`, add `cancelEntrySessionReminders()` sweeping BOTH `break` AND `session-start` via `cancelByKind`, awaiting `breakScheduleInFlight` (audit #30, REUSED). On session END: reschedule the daily reminder via `scheduleSessionStartReminder(preferred, {request:false})` threading `PreferredTime` + `sessionStartReminderEnabled`. Tests: (1) cancelled on the EXISTING entry paths (launch + onResume); (2) still EXISTS with the RIGHT hour after a session for an enabled+granted user; (3) for an enabled-but-DENIED user the reschedule is a clean no-op; (4) the break interaction does not reintroduce audit #30. NOT a focus.tsx wiring. Cheap — reclaimed time → M7.recruit.
- **M7.0a — `firestore.rules` + `analytics_events` sink (Days 1-3; PRE-WRITTEN Day 0).** Create-only/no-free-text `analytics_events` sink modeled on `training_samples` (line 36), with the WRITE-IDEMPOTENCY key (Critic A-8). Partner-read EXACT-PATH `/users/{uid}/social/summary` + `/users/{uid}/social/profile` — never `=**`, never the owner-grant (lines 24-26), DENY the raw `users/{uid}` doc. **RELEASE-GATE TESTS (Critic B-1/B-2): (i) the sorted-UID `isPartner()` partner-read predicate proven (partner reads; non-partner/removed/blocked DENIED); (ii) the `acceptInvite` partnership-CREATE rule's `get()` count enumerated and proven UNDER the Spark 10-cap.** Shape-validate `presence` + name-projection (`hasOnly`). WRITE predicate: `partnerships/{pairId}` creatable ONLY against a currently-valid invite the OTHER party issued, landing `active`; reads BOTH pointers. **NEW: the NARROW cross-tree unpair write-grant — a member may flip THEIR partnership to `ended` + null the counterpart's `users/{otherUid}/partner` pointer (Critic B-3), tested (non-member denied).** F-lifecycle DELETE rules. The per-code broadcast edge-cap enforced at claim-time (Critic A-4). NO `co_sessions` write rule. DEFERRED: the SNAPSHOT-read get()-count (NOT the create-rule budget). Backward-compatible, sign-off-gated. **OLD-CLIENT × NEW-RULES WINDOW (Critic A-6/B-5): verify the prior-binary JS client (still running between rules-deploy and OTA-propagation) writes NOTHING the new additive rules reject — an explicit emulator/manual check, not just "additive" by assertion.**
- **M7.1 — Invite-code install-and-pair backend + first-class broadcast funnel + OFFLINE state (Day 1-3).** `partner_invites/{inviteId}` 6-char code (single-use best-effort for 1:1; MULTI-claim + per-code edge-cap for broadcast — Critic A-4; ≤72h; collision-checked; revocable); `acceptInvite(code)` idempotent on `pairId`, reading both pointers; **the issuer-notify spam-cap written OUTSIDE the transaction with its own idempotency guard (Critic A-2) so a transaction auto-retry does not double-fire the notify and is not serialized into a contention hotspot;** the OFFLINE/Retry state (F1-states-j); re-pair/block-evasion/dead-issuer; the inviter-oriented `partner_dormant`; the want-a-partner intent doc; install→auth→paired seam. Singleton transaction + the two rule predicates pinned by Day-1/Day-2 emulator tests S can run. `claimed_via_broadcast` first-class.
- **M7.2 — Title-stripping `social/summary` + DECOUPLED display-name projection + `pair_context` tag (Critic A-1).** On `session/finalize.ts`, write `social/summary` copying ONLY {minutes, focus_score, ended_at, phase_at_end}, OMITTING `task.title`; log `pair_context`. **The name projection (`social/profile`) is DECOUPLED from finalize: a `projectDisplayName(uid)` writer called on PAIR-ACCEPT and from `updateDisplayName` (userProfile.ts:64), applying length-cap + control-char strip + a content (phone/URL/slur) heuristic. Finalize only refreshes it.** Unit test: no title; bounded + content-checked name; a dormant friend HAS a projected name.
- **M7.0 — Partner edge + SINGLE pairing-consent (F3a) + mute + REMOVE/BLOCK + singleton pointer + lifecycle.** `partnerships/{pairId}` (sorted-UID, `members[2]`, `status` incl `active`/`ended`/`partner_dormant`, `created_at`, `session_epoch`, `share_consent` default false — ONE flag covering summary+presence, `muted_by`, `blocked_by`) + `users/{uid}/partner` singleton pointer, `partners.ts`. `removePartner` (one-tap, clears both pointers via the cross-tree grant) / `blockPartner` (CONFIRM); both READ-RULE revocation (Critic A-3) + client listener teardown + cache-invalidation.
- **M7.lifecycle — WIPE/DELETE completeness (PRIORITY-A, never-cut; Critic B-3).** Extend wipe + account-deletion via a PARALLEL single-doc deleter to delete `social/summary`, `social/profile` name projection, sentiment, report, `presence/{uid}`, the singleton pointer, the want-a-partner intent doc; flip co-owned `partnerships` to `ended` clearing the other pointer (via the narrow cross-tree grant). NO co_sessions, NO aggregate survivor. Emulator test: a wiped user is unreadable by their ex-partner across ALL surfaces INCLUDING the name; the cross-tree grant is member-only. Reuses the tombstone pattern.
- **M7.3 — Presence sync + IN-APP finish card + cancel-on-entry integration + STALENESS (PROTECTED, Days 2-3; Critic A-3).** Session-end → presence write + reschedule the daily reminder (threaded). Sign-out → presence `idle`. Presence carries `started_at` + `ended_at` (staleness + `just_finished` decay). Session-entry sites use the GENERALIZED helper (swapped by S). The F4 finish surface is the IN-APP card from synced `social/summary` — NO backgrounded notification. The static `shouldShowBanner:true` becomes a session-state-aware closure (foreground edge only). OS permission on SECOND session-end / first-reaction.
- **M7.0b — Presence Room schema (dark).** `rooms/` schema behind `FLAG_ROOM_LIVE=false`. NO F-coflog writer. Pure service + rules; no UI. Slips first.

### Mustafa (S7.x) — all UI
- **S7.0 — Partner data-layer UI + pairing states (PRIORITY-A).** Invite/accepted + F1-states; `partner_dormant` shows the INVITER "{name} joined — hasn't focused yet" + resend (per mobile/CLAUDE.md:102, name from the pair-accept projection); **the OFFLINE/Retry state (F1-states-j — Critic A-2);** the dormant FRIEND sees their own normal solo experience; NO issuer-ack; partner view (summaries from `social/summary` + name from `social/profile`, NOT the owner doc); the SINGLE consolidated pairing-consent (F3a, default OFF); mute (F2b) + the optional mute-reason + listener teardown; REMOVE one-tap / BLOCK confirm (F2c); the no-friend "Skip" + want-a-partner intent (F1-skip); the issuer-notify surface + one-tap REPORT. Fires `logEvent` + the ignition counter.
- **S7.notif-swap — swap the TWO existing `cancelBreakReminder()` calls for M's `cancelEntrySessionReminders()` (S's lane).** `useStartSession.ts:43` + `RestoreSessionPrompt.tsx:52`. ~2 lines each. M reviews. NOT a focus.tsx change.
- **S7.perm — SECOND-session-end / first-reaction OS permission prompt (REUSE `ensurePermission`).** NOT at pairing, NOT on the first reward beat.
- **S7.route — deep-link (`floq://` only) + notification-tap navigation. NOTE (Critic B-6): `floq://` cannot open an uninstalled app, so pairing is 100% typed code at cold install; the route handles the warm-app case and the field is the primary cold-install path.**
- **S7.1 — The card = a SHAREABLE invite artifact + a SECOND calm invite home in the Partner tab (PRIORITY-A).** Wire the W6 card's share to F1's multi-claim broadcast code (F-broadcast-supply); ALSO surface the invite in the Partner tab as a deliberate "find a partner" affordance.
- **S7.2 — Beat 1 LIVE-async surface (staleness-gated incl. sign-out/just_finished decay) + listener under the tab layout, torn down on mute/block + ignition counter + `started_via` (PROTECTED).** Silent when no partner is fresh-live.
- **S7.3 — Beat 2 frozen-snapshot chip with TRUE-AT-START copy (PROTECTED).**
- **S7.4 — Beat 3 reward surfaces + invite home + sentiment prompt + IN-APP "what your partner did" finish card (F4-inapp) + fire/clap (F6, rate-capped, context-anchored, self/double/mutual enumerated — Critic A-7).**
- **S7.6 (optional polish) — pair-insight UI.** First to cut (absorbs Days-2-3 compression).

**Handoff contract (Day-0/Day-1 unblock):** M publishes Day 0 the TS signatures of `logEvent`/`analytics_events` shape (incl `seq`, `kind`, the idempotency key, the ignition counter, `claimed_via_broadcast` — NO `arm`), `cancelEntrySessionReminders()` AS A GENERALIZATION of the existing break-cancel helper + the TWO EXISTING call sites + the threaded reschedule signature (NOT a focus.tsx wiring), `partners.ts` (removePartner/blockPartner via the cross-tree grant, the inviter-oriented `partner_dormant`, the offline-claim state, issuer-notify-spam-capped OUTSIDE the transaction, bulk-revoke, the singleton pointer, the want-a-partner intent — NO `ackPairing`), `presence` writer (incl. sign-out idle + `ended_at` decay) + staleness-bounded LIVE read + listener-mount contract, `social/summary` + the DECOUPLED `projectDisplayName` writer (pair-accept + edit) + `pair_context` tag, the IN-APP finish-card read contract, `acceptInvite(code)` + first-class broadcast funnel (multi-claim + edge-cap) + the SINGLE `share_consent` flag + the `floq://` contract as stubs. S never edits rules.

---

## 5. Open tensions — resolved

| Tension | DECISION | Reason |
|---|---|---|
| **T0 — L11 capacity + RELOCATION** | Day 0 FORCES reconciliation + amendment (A0). Capacity-tiering DELETED. 1-2 M-days costed against the move; scope to ~5-6 effective M-days. The reclaimed notif-fix day buys slack toward the Egypt SPOF. A recruiting slip SLIPS THE BETA. | A verified move outranks a doc line; the falsely-budgeted redesign was costing the most-constrained engineer. |
| **T-supply — recruit-FIRST + the recruiting funnel instrumented (Critic A-2 v13 + Critic A-5)** | F-recruit worked from Day 0; the ask→yes→sent→installed tally is kept so Day 5 separates "loop broken" from "not enough asks"; broadcast promoted to first-class supply; meter/tripwire/terminal DEMOTED. NO arm. | The numerator dies the read, and you can't read the numerator without its own top-of-funnel denominator. |
| **T-notif-layer — the cancel fix is a REUSE (VERIFIED)** | M generalizes the EXISTING cancel-on-entry helper; S swaps the two EXISTING call sites. NOT at focus.tsx. | The two-site symmetric pattern already EXISTS with a "symmetric" comment. |
| **T-name-projection — DECOUPLE from finalize (Critic A-1, NEW, VERIFIED)** | The name projection fires on PAIR-ACCEPT + on `updateDisplayName` EDIT (not only finalize), with a content check; the dormant `{name}` comes from the pair-accept write. | `updateDisplayName` (userProfile.ts:64) is editable any time and finalize-only projection (a) renders the dormant fix BLANK and (b) lets a post-pair rename poison the partner-visible name unchecked. |
| **T-rules-release-gate — the two transit predicates are NOT W9 (Critic B-1/B-2, NEW, VERIFIED)** | The `acceptInvite` CREATE-rule get()-budget (Spark 10-cap) AND the sorted-UID `isPartner()` partner-read predicate are pulled onto Day 1-2 as release gates with their own emulator tests, before UI. The SNAPSHOT-read get()-cost stays W9. | If either rule is wrong the Axis-A read returns ZERO; they are the unreviewed code in the Egypt window; v13 conflated the (deferrable) snapshot cost with the (release-blocking) create-rule budget. |
| **T-wipe-cross-tree — named M-work, not free reuse (Critic B-3, NEW, VERIFIED)** | A PARALLEL single-doc deleter ships (`deleteSubcollection` is `'sessions'|'tasks'`, subcollection-only); a NARROW member-only cross-tree write-grant lets a member flip the co-owned partnership + null the counterpart pointer, tested. | The owner-only rule (lines 22/25) forbids writing the ex-partner's tree, so the "never-cut" wipe was a correctness hole. |
| **T1 — Room: scaffold-dark vs LIVE seed? (REJECTED)** | Scaffold-dark, SCHEMA-ONLY. Zero Room signal NAMED as the §9 cost. The F1-skip want-a-partner intent is the first seed the dark Room later consumes. | A live Room at W7 n is a ghost town → false negative; §9 says don't bet the scarce week on B. |
| **T2 — Friend-only vs stranger seed?** | Strictly friend-only NORM (L18). The broadcast claim opens a COARSE-ONLY consented edge, multi-claim with a per-code edge-cap (Critic A-4), issuer NOTIFIED + BULK-REVOCABLE — first-class SUPPLY. | A consented capped coarse claim is both a free k>1 probe AND the only pairs-without-M's-hands path; the cap is prevention not just cleanup. |
| **T3 — Egypt ownership / SPOF** | PRIORITY-A; PROTECTED CO-PRESENCE Days 2-3; DEFERRABLE absorbs the Days-2-3 compression FIRST. The SPOF is NAMED: the two transit rule predicates + the singleton transaction are test-pinned; rules pre-written Day 0; the slip is the pressure valve. Axis B DEFERRED. | A clean pairing read is bankable; the over-booking is real (Critic B-3) and gets concrete mitigations + an honest slip, not a calendar caveat. |
| **T4 — Harden pairing rate vs accept + instrument?** | HARDEN AND GENERATE: F1 funnel + singleton guard + the release-gate rule tests + recruit-from-Day-0 + the recruiting-funnel tally + broadcast-supply. Build the meter but DEMOTE it. | The bottleneck is humans-in-pairs; gold-plating the meter while the supply was a footnote was the inversion. |
| **T5 — Coupling gentleness / forward commitment?** | Pure join-now, zero forward commitment. Visible pair streak SCAFFOLD-DARK. | The streak is the lock-in, OFF; Axis B deferred so no causal read to pollute. |
| **T6 — Consent & annoyance & FELT experience** | Pairing collapsed to ONE consent decision (F3a). OS permission at SECOND session-end / first-reaction. Reversible mute (+ teardown + optional reason); REMOVE one-tap / BLOCK confirm; IN-APP finish card; reactions context-anchored + fully enumerated (Critic A-7); LIVE-but-staleness-bounded (incl. sign-out/just_finished decay); sampled sentiment; a discoverable REPORT path. | Armors against manufactured feeling, calm-by-emptiness, a permanent-lie/residual-beacon presence, consent-fatigue, AND App-Store UGC rejection. |
| **T7 — Push delivery mechanism** | Remote push CUT. Delivery = local `scheduleNotificationAsync` (daily reminder) + in-app. The realtime join loop + timely-reaction push = W9. | The survivors need no remote push; the timely-reward cost is NAMED. |
| **T8 — OTA coherence + RULES-DEPLOY ORDER + EXISTING-COHORT MIGRATION + OLD-CLIENT×NEW-RULES (KEPT + Critic A-6/B-5)** | W7 ships OTA. Day-0: confirm appVersion + channel match. Rules deploy FIRST (additive). Day-7: deploy rules, verify, THEN `eas update`. The existing cohort IS the beta: first-open idempotent resync reschedules the daily reminder, reconciles a mid-session user, hydrates the absent `partner` pointer as null, NO re-onboarding. **NEW: an explicit old-client × new-rules check — the prior-binary JS (running until OTA propagates on cold boot) writes nothing the new additive rules reject.** A Day-6 gate exercises the OTA on an already-onboarded binary. | The OTA trap needs a matching binary, a server-side gate, a migration for installed users, AND verification of the transition window the new-client-only gate misses. |
| **T9 — Single native-build SPOF** | DISSOLVED: NO native rebuild. The installed binary's daily-trigger shape is VERIFIED Day 1. | Cutting push removes the SPOF; the notif fix is JS at sites that already cancel-on-entry. |
| **T10 — Sequencing + RECRUIT-FIRST + the Days-2-3 serialization NAMED (Critic B-3)** | See §8. Recruiting Days 0-7; the notif REUSE frees a day; PROTECTED co-presence Days 2-3; rules Days 1-3 (pre-written Day 0). The Days-2-3 backend stack is acknowledged as the dominant scheduling risk; DEFERRABLE absorbs it first; the Day-5 slip is the valve. | v13 deferred the numerator and gold-plated the denominator; this round inverts it AND stops pretending the compressed backend stack closes without a slip valve. |
| **T11 — Co-session detector (CUT)** | F-coflog runtime writer CUT to PURE W9 SCHEMA. The graph-asset clock does NOT start in W7 — NAMED (openFlaws). | Zero W8 read value, costs rules+writer+tz+dedup+lifecycle. |
| **T12 — Felt-experience instrument** | F-sentiment ships, sampled, owner-only, judging the SESSION + the `pair_context` tag. Solo-parity CUT. AMBIENT-PRESSURE read via mute-rate, NOT sentiment. | No causal arm to protect; session-sentiment can't see the presence-surface pang. |
| **T13 — Title leak (VERIFIED, KEPT)** | Partner-read points at the title-stripping `social/summary`, exact-path. Rules test asserts DENY on `sessions/{id}`. | The lowest-effort partner-read would read titles from a live title-bearing doc. |
| **T13b — Display-name free-text leak + MUTABLE source (VERIFIED, KEPT + Critic A-1)** | The partner-visible name is a length-capped, control-char-stripped, CONTENT-CHECKED projection on `social/profile`, written on pair-accept + edit; rules DENY the owner doc; F-lifecycle wipes it; a REPORT path exists. | `display_name` (userDoc.ts:18) is free text editable any time (userProfile.ts:64); a finalize-only projection leaves it stale/poisonable and the dormant fix blank. |
| **T14 — Mid-session enforcement at the existing layer (VERIFIED, KEPT)** | The cancel GENERALIZES the EXISTING two-site `cancelBreakReminder` pattern to sweep `session-start`; reschedule-on-END threaded + both-branches test. NOT at focus.tsx. | A scheduled notification fires backgrounded; the fix is ~10 lines at sites that already solve the break case. |
| **T15 — Invite forgery / enumeration + SINGLE-USE + ONE-PARTNER atomicity + the CREATE-rule get()-budget (KEPT + Critic B-2)** | Write-rule: `partnerships` creatable ONLY against a valid invite the OTHER party issued; one-partner via both pointers in the transaction; the create-rule get()-count proven UNDER the Spark 10-cap as a RELEASE GATE; single-use BEST-EFFORT (1:1) / multi-claim+cap (broadcast). Pinned by Day-1/Day-2 emulator tests S can run. | A guessable write is a stalking primitive; an atomicity guard with no reviewer needs a test; a create-rule over the get()-cap returns ZERO on the whole read. |
| **T16 — Instrumentation + DETECTOR HONESTY + WRITE-IDEMPOTENCY (KEPT + Critic A-8)** | Build the `analytics_events` sink (the meter not the product), with a deterministic eventId idempotency key so retry-on-reconnect overwrites not double-writes; per-device `seq` + `kind`; the `seq`-gap detector is an OFFLINE W8 analysis, churn-blind. | A create-only sink with retry and no idempotency key inflates every funnel rate §7 reads. |
| **T17 — Recruitment threshold (KEPT VERBATIM) + the recruiting funnel (Critic A-5)** | A loop-readability threshold (~10-16 pairs); below it the read is anecdote → SLIP. Recruit from Day 0; tally ask→yes→sent→installed so the slip diagnosis isn't blind. | A power calc you can't run is theater; size for the read, GENERATE supply, and instrument the generation. |
| **T18 — Join-conversion: PURE DIAGNOSTIC (KEPT)** | `started_via:'partner_ignition'` is a PURE DIAGNOSTIC; decision-bearing = the Axis-A loop-fire metrics + `pair_context`. | A calm-by-design surface yields a crippled-by-design number. |
| **T19 — Ignition language lies + EMPTINESS + PERMANENT-LIE + RESIDUAL-BEACON + PRESSURE (KEPT + Critic A-3)** | Renders ONLY when FRESH-live-focusing; silent otherwise; sign-out → idle; `just_finished` decays. The social-obligation FEELING is NAMED, read via mute-rate. | "join me?" lies; a dead read-receipt; an uncleared `focusing`/`just_finished`/signed-out beacon is a permanent lie; AND the surface is a pressure engine — all acknowledged. |
| **T20 — Finish signal: IN-APP, NOT a digest (KEPT) + reaction timeliness** | The finish signal is an IN-APP "what your partner did" card. Digest CUT. The reaction-arrives-cold cost is NAMED; the surface re-anchors to the original session; the timely push is W9. | Deletes a concurrency subsystem off the relocating engineer with no loss to Axis A; the timeliness cost is honestly booked. |
| **T21 — Blocked-party mystery + block-evasion + CONFIRM + TEARDOWN + READ-RULE revocation (KEPT + Critic A-3)** | REMOVE → honest neutral (one-tap); BLOCK → indistinguishable, CONFIRM; both tear down the client listener AND revoke via the partner-read RULE (presence is one owner-written doc, not per-partner-written). Only SAME-UID block-evasion closed. | A dangling listener/read on a blocked partner's presence is the stalking primitive BLOCK kills; the revocation is a read-gate, which v13 muddled as a write-gate. |
| **T22 — Public-code stranger admission + SUPPLY + ABUSE CEILING (KEPT + Critic A-4)** | Issuer-ack REMOVED. Coarse-only edge; consent default-OFF; REMOVE/BLOCK; issuer NOTIFIED on first claim (capped, OUTSIDE the transaction); BULK-REVOKE. Multi-claim with a per-code outstanding-edge cap + short TTL (prevention, not just cleanup). First-class SUPPLY. | A consented capped coarse claim is the only organic supply path; single-use broke group-chat supply; an uncapped multi-claim is an open firehose — the cap closes it for App-Store 1.2. |
| **T23 — Wipe blindness + NAME + POINTER + INTENT + the cross-tree grant (KEPT + Critic B-3)** | F-lifecycle PRIORITY-A never-cut via a parallel single-doc deleter: enumerate+delete `social/summary`, the name projection, sentiment, report, `presence`, the singleton pointer, the want-a-partner intent; flip co-owned `partnerships` to `ended` + null the counterpart pointer via the narrow member-only cross-tree grant; emulator test; Day-6 PASS-1. NO co_sessions, NO aggregate survivor. | A privacy regression on erase can't ship; the cross-tree write was a correctness hole; the moat cost is booked. |
| **T24 — get()-per-snapshot cost (DEFERRED) vs the CREATE-rule budget (NOT deferred — Critic B-2)** | The cross-doc SNAPSHOT-read get() cost is real but at n≤8 won't bite functionally; that assertion DEFERRED to W9. The CREATE-rule get()-budget (Spark 10-cap) is a Day-1/Day-2 RELEASE GATE. | W9 cost-modeling for the read path; but the write-gate's get()-count, if over the cap, zeroes the entire read — that cannot wait. |
| **T25 — k>1 / ceiling — NAMED (KEPT)** | F-coflog writer CUT; the graph-asset clock does NOT start in W7. ONE k>1 read on Axis A (broadcast-card claim). The "Low A → build the Room" branch rests EXPLICITLY on §9 conviction + this datapoint, and the plan SAYS SO. | The honest move is to name the §9 cost, not dress a dark schema as a network hedge. |
| **T26 — Decision matrix terminal (KEEP VERBATIM) + Axis B deferral (KEPT)** | KEEP VERBATIM the "below the loop-readable threshold the question is UNANSWERED — do NOT launder into 'build the Room,' do NOT pass/kill → slip and re-recruit." Axis B DEFERRED to W9; the "paired ≤ solo" trigger DELETED for W7. | The cleanest thinking; the undefined-arm trigger is unfireable. |
| **T27 — Orphaned/crashed session state (VERIFIED, KEPT)** | Presence carries `started_at`; F3 renders only if FRESH so a crashed `focusing` reads idle; sign-out sets idle; `just_finished` decays via `ended_at`. | VERIFIED sessions survive app-kill (`restore.ts`); without bounds F3's "true at render" is false the moment any session crashes/sign-out/staleness occurs. |
| **T28 — The dormant-friend felt failure + its NAME dependency (VERIFIED, KEPT + Critic A-1)** | RESTORE the inviter-oriented "{name} joined — hasn't focused yet" + resend (per mobile/CLAUDE.md:102); the `{name}` is supplied by the PAIR-ACCEPT projection so it is never blank; do NOT narrate flakiness TO the friend. | v13 optimized the dormant case for analytics at the cost of the activated evangelist; and its name source (finalize-only) would have rendered the fix blank. |
| **T29 — Invite buried in the drained reward beat (KEPT)** | The reward card stays a shareable artifact; the invite ALSO gets a calm home in the Partner tab. | The post-session user is spent — the worst moment to ask for the dominant-multiplier growth action. |
| **T30 — Consent fatigue at pairing (KEPT)** | COLLAPSE data-read + ping-consent into ONE pairing-consent (F3a); OS permission stays separate (deferred to second-session-end). | Three micro-decisions at the connection moment contradicts the "immediate partner" thesis. |
| **T31 — Skip's dead-end return (KEPT)** | F1-skip adds a non-inert want-a-partner intent doc + names the Skip-then-return §7 health guard. | The partnerless user is the entire reason the Room exists. |
| **T32 — Offline at the install→pair seam (Critic A-2, NEW, VERIFIED)** | F1-states-j: an explicit offline/Retry state, the typed code persisted for one-tap retry, a clear non-paired-yet status until the transaction commits. | `acceptInvite` is a transaction; the most common real-world failure is the network, and a silent spinner fails the activated evangelist's friend. |
| **T33 — Reaction edge-cases under-scrutinized (Critic A-7, NEW)** | F6 enumerated: self-react disallowed; double-react idempotent; mutual react well-defined (cap per-direction); pre-consent stranger react gated. | F6 got less scrutiny than F1-states' a–i rigor; the asymmetry was a real gap. |

---

## 6. decisions.md amendments to lock FIRST (Day 0, before any code)

- **A0 — Reconcile L11 capacity + COST THE RELOCATION (FIRST).** VERIFIED L11 (line 84). Record: (i) L11 holds → capacity-tiering void; OR (ii) reduced → SUPERSEDES L11. EITHER WAY, 1-2 M-days costed against the move; scope to ~5-6 effective M-days. A recruiting slip SLIPS THE BETA. No build code until A0 is recorded.
- **A1 — Amend L2 / async thesis: permit real-time coarse co-presence.** L4 unchanged and reinforced — load-bearing on the EXACT-PATH `social/summary` projection AND the content-checked name projection. Coarse presence (real-time `onSnapshot`, `state:focusing` LIVE, staleness-bounded incl. sign-out/just_finished decay) + finish summaries MAY leave to an active partner, gated on the SINGLE pairing-consent (F3a). NO co-occurrence log written in W7.
- **A2 — Enumerate the allowed coarse payload + the name-projection egress + the one-partner pointer + the want-a-partner intent + the single-use/multi-claim honesty + the broadcast edge-cap.** Names `presence/{uid}` + `social/summary` (EXACT path) + the content-checked `social/profile` name projection + `analytics_events` as the ONLY partner-egress points; forbids everything else via shape validation; DENIES the raw `users/{uid}` owner doc. The sentiment + report + want-a-partner-intent docs are OWNER-ONLY. The partnership-create requires a valid invite the OTHER party issued, landing `active` — NO issuer-ack — issuer NOTIFIED (capped, OUTSIDE the transaction) + BULK-REVOKE; one-partner via both pointers in the transaction (test-pinned); the CREATE-rule get()-count proven UNDER the Spark 10-cap (release gate); single-use BEST-EFFORT (1:1) / multi-claim + per-code edge-cap (broadcast). NO `co_sessions` egress, NO aggregate survivor. The SNAPSHOT-read get()-count assertion DEFERRED to W9.
- **A3 — New L: "No notification during a session" is a hard invariant, ENFORCED BY GENERALIZING THE EXISTING CANCEL-ON-ENTRY HELPER AT THE EXISTING ENTRY SITES (VERIFIED).** VERIFIED the daily `session-start` reminder is NOT swept today; VERIFIED the entry sites (useStartSession.ts:43, RestoreSessionPrompt.tsx:52) ALREADY sweep `break` with a "symmetric" comment. `cancelEntrySessionReminders()` generalizes them to also sweep `session-start`, reusing `breakScheduleInFlight`; reschedule-on-END threads preferred-time + enabled-pref + a both-permission-branches test. NOT inside `startSession`, NOT at focus.tsx. The only W7 notification is the daily reminder; the finish signal is IN-APP.
- **A4 — Capacity governed by A0. Axis B DEFERRED to W9. RECRUIT-FIRST + the recruiting funnel instrumented.** Build-RISK priority (§4): PRIORITY-A as listed; PROTECTED CO-PRESENCE Days 2-3; DEFERRABLE absorbs the Days-2-3 compression first. Recruiting starts Day 0 with an ask→yes→sent→installed tally; the meter/tripwire/terminal DEMOTED. The Axis-B causal verdict is deferred BY DESIGN.
- **A4b — Remote push CUT; the finish digest CUT to in-app; delivery = the daily local reminder + in-app; the realtime join loop + the timely-reaction push + the Cloud Function are the FIRST W9 spend.** `aps-environment` ALREADY EXISTS. W7 ships OTA. Native analytics CUT.
- **A4c — Consent & first-touch & FELT-experience invariants.** (i) ONE consolidated pairing-consent, default OFF; (ii) OS permission at SECOND session-end / first-reaction; (iii) sender gets NO promise-of-delivery surface; (iv) chip copy true-at-start; (v) REMOVE one-tap / BLOCK confirm, both READ-RULE-revoked (Critic A-3) + listener-torn-down; (vi) sentiment owner-only, session-judging, sampled — solo-parity CUT; ambient-pressure read via mute-rate; (vii) finish signal IN-APP, reactions context-anchored + fully enumerated (Critic A-7); (viii) a claim lands the friend ALREADY PAIRED on a COARSE-ONLY edge — NO issuer-ack — issuer NOTIFIED (capped, outside the transaction) + BULK-REVOCABLE, multi-claim with a per-code edge-cap (Critic A-4), promoted to SUPPLY; (ix) Beat 1 renders only when FRESH-live, staleness-bounded incl. sign-out/just_finished decay (Critic A-3), pressure NAMED; (x) `partner_dormant` keeps the INVITER oriented with "{name} joined — hasn't focused yet" + resend, `{name}` from the PAIR-ACCEPT projection (Critic A-1); (xi) the partner-visible NAME is a content-checked, length-capped projection written on pair-accept + edit (Critic A-1), with a REPORT path; (xii) the invite has a SECOND calm home in the Partner tab; (xiii) the no-friend Skip writes a non-inert want-a-partner intent; (xiv) the install→pair seam has an explicit OFFLINE/Retry state (Critic A-2).
- **A4d — F-coflog CUT to PURE W9 SCHEMA.** No W7 runtime writer/write-rule/lifecycle obligation. The graph-asset clock does NOT start in W7 — NAMED as the §9 moat cost.
- **A4e — Instrumentation is a spine item but the METER not the product.** The create-only sink + `logEvent`; a deterministic eventId idempotency key (Critic A-8); per-device `seq` + `kind` + flush-on-reconnect; MMKV "surface seen"; the `seq`-gap detector is an OFFLINE W8 analysis, churn-blind; a `claimed_via_broadcast` tag. NO `arm`. Firebase Analytics NOT used. NO powered-floor gate. DEMOTED.
- **A4f — Pair supply is the M-OWNED SPINE, WORKED FROM DAY 0, with its OWN funnel tally (Critic A-5).** A named recruit list recruited in parallel with code from Day 0, sized for the Axis-A loop read; the ask→yes→sent→installed tally kept daily; the broadcast claim a first-class organic-supply mechanic; a Day-5 shortfall SLIPS THE BETA (last resort), never fires L18. No arm.
- **A4g — RULES DEPLOY BEFORE OTA + EXISTING-COHORT MIGRATION + OLD-CLIENT×NEW-RULES CHECK + rules PRE-WRITTEN Day 0.** `firestore.rules` (additive) deploy FIRST + verified; THEN the OTA. The first-open resync reschedules the daily reminder, reconciles a mid-OTA-session user, hydrates the absent `partner` pointer as null, requires NO re-onboarding; **the prior-binary JS (running until OTA propagates) writes nothing the new additive rules reject — an explicit check (Critic A-6/B-5);** a Day-6 gate exercises the OTA on an already-onboarded binary. Rules pre-written Day 0.
- **A5 — New L (W7 scope lock):** SHIP the notif cancel-on-entry GENERALIZATION + F-recruit (Day-0, loop-readable, NO ARM, funnel-tallied) + F-broadcast-supply (multi-claim + edge-cap) + F0 (write-idempotent, DEMOTED) + F1/F1-states(inviter-oriented dormant; OFFLINE state; NO issuer-ack; capped notify outside the transaction; bulk-revoke; dead-issuer; no-friend pass-through + want-a-partner intent; coarse-only-edge; singleton guard + the two release-gate rule predicates test-pinned)/F1-skip/F2/F2-name(DECOUPLED content-checked name projection + report)/F2b(+ambient-pressure read)/F2c(REMOVE one-tap + BLOCK confirm + read-rule revocation + teardown)/F3(live-async, staleness-bounded incl. sign-out/just_finished decay, pressure-NAMED, PROTECTED)/F3a(ONE consent)/F4-inapp(IN-APP card, reaction-timeliness NAMED, PROTECTED)/F6(rate-capped, context-anchored, fully enumerated)/F7(minimal write incl. sign-out idle + ended_at decay + delete + staleness read)/F-notif(generalized helper, PROTECTED)/F-sentiment(session-judging, owner-only, solo-parity CUT, `pair_context`)/F-lifecycle(wipe-completeness via a parallel single-doc deleter + the cross-tree unpair grant, no co_sessions, no aggregate survivor) + M7.2. SCAFFOLD-DARK F5-flag + F8(schema-only). DROP F-notif-redesign + F-issuer-ack + F9 + F1b + F-push + F-cloudfn + native analytics + F5-engine + the F4 digest + F-coflog runtime writer + the solo-parity machinery + the randomized arm + the single-use-broadcast enforcement + the ≥10-min detector + the two extra counters + the W7 aggregate survivor + the live Room + monetization + the capacity-tiering framing + the "rules-layer single-use" claim. Supersedes M7.1b. Pre-commits the §7 Axis-A read + correctness bars + loop-readable threshold + decider (Mohamed); Axis B deferred to W9; keeps the L18 revert one-step (A6).
- **A6 — L18 one-step-revert teardown.** (i) `analytics_events` RETAINED but bounded by the F-lifecycle per-member delete path (NO co_sessions, NO aggregate survivor); (ii) live partnerships degrade via `FLAG_SOCIAL_LIVE=false` → neutral terminal; (iii) the exact-path partner-read + name-projection + the cross-tree unpair RULES may stay. "One-step" = flip the flag.
- **A7 — AXIS B DEFERRED TO W9.** The randomized solo control is operationally impossible at this n/team; Axis B (causal per-pair retention) DEFERRED to the larger W9+ cohort; W7 recruits for the Axis-A loop read only; no arm, no "paired ≤ solo" trigger this week. The `pair_context` tag is banked. Locked Day 0 because it changes WHO/HOW MANY M recruits.
- **A8 — UGC moderation/report surface.** W7 opens stranger-reachability (broadcast-card coarse edge + projected display names), so a discoverable REPORT path + the REMOVE/BLOCK/bulk-revoke set + the content-checked name projection + a consent-to-UGC acknowledgment ship as a TestFlight/App-Store guideline-1.2 dependency.

---

## 7. W8 read — AXIS A (LOOP MECHANICS) IS THE W7 DELIVERABLE; AXIS B (CAUSAL RETENTION) DEFERRED TO W9; + ONE k>1 PROBE

**The reframe (KEPT):** W7 produces ONE read this team can ship CLEAN — Axis A: did the loop machinery FIRE and does it COMPOUND. Axis B is DEFERRED to W9, BY DESIGN. F-recruit is sized for loop-readability AND worked from Day 0 with its own funnel tally (Critic A-5) so the supply exists to read AND a thin reading is diagnosable. Consistent with strategy §9 + §4. The `pair_context` tag is banked for W9.

**Pre-conditions for the Axis-A read to be valid (correctness bars — fix-and-re-read; do NOT fire L18 on an artifact):**
- A0 reconciled WITH the relocation buffer. A7 Axis-B-deferral recorded.
- Rules deployed before the OTA + verified; the old-client × new-rules window checked (Critic A-6/B-5).
- The existing-cohort OTA migration verified — a Day-6 gate on an already-onboarded binary with a scheduled daily reminder.
- **The two RELEASE-GATE rule predicates proven (Critic B-1/B-2): the `acceptInvite` CREATE-rule's get()-count is UNDER the Spark 10-cap, and the sorted-UID `isPartner()` partner-read predicate grants the partner / denies non-partner/removed/blocked — each with its own emulator test.**
- The sacred-middle invariant verified: a session straddling the preferred-time reminder does NOT fire it, INCLUDING a RESUMED session — on the shipping binary, with the cancel as the GENERALIZED helper at the EXISTING entry sites.
- The daily reminder survives a session across BOTH permission branches.
- F-recruit hit the loop-readable threshold (or honestly slipped) — diagnosable via the ask→yes→sent→installed tally.
- The `analytics_events` sink is live + funnel events land + the idempotency key prevents retry double-writes (Critic A-8); the `seq`-gap detector is OFFLINE W8, churn-blind — absence is ambiguous, never zero.
- F1 land-paired reliability ≥ 90% (NO issuer-ack in the denominator), decomposed by failure-reason INCLUDING the offline-claim reason (Critic A-2).
- No title in `social/summary`; the partner-visible name is the content-checked projection written on pair-accept + edit (Critic A-1), NOT the owner doc; a dormant friend HAS a projected name.
- The finish surface is an IN-APP card; the daily reminder is CANCELLED while in-session — verified including a resumed session.
- Beat-1 presence is staleness-bounded: a crashed session's uncleared `focusing` does NOT render "focusing now"; a signed-out partner reads idle; a stale `just_finished` does not render "just finished" (Critic A-3).
- Wipe-completeness verified: a wiped/deleted user is unreadable by their ex-partner across `social/summary` + the NAME projection + sentiment + report + `presence` + the singleton pointer + the want-a-partner intent doc, AND the cross-tree unpair grant is member-only (Critic B-3).
- `partner_dormant` pairs: the INVITER sees the oriented "{name} joined — hasn't focused yet" state (name non-blank), excluded from per-pair LOOP denominators (tagged).

### Axis A — PAIRING RATE / loop mechanics + ONE k>1 PROBE (THE W7 DELIVERABLE)
- **Recruiting funnel (Critic A-5, NEW):** ask → said-yes → code-sent → installed-and-paired, tallied daily. The denominator that lets Day 5 separate "loop broken" from "not enough asks."
- **Code-claim convert** (invite issued → installed + land-paired): directional ≥ 50% / weak < 25%. No issuer-ack round-trip; no pairing attributed to the `floq://` link (it's 100% typed code — Critic B-6).
- **Land-paired reliability** ≥ 90% (correctness bar), failure-reasons including offline.
- **Time-to-pair** per pair.
- **Reactions happen** (finished sessions surfaced → ≥1 reaction): directional ≥ 40% / weak < 20% — denominator excludes `partner_dormant`. (Reactions arrive cold via in-app; a low rate may reflect the timeliness gap, read with that caveat.)
- **Card share rate:** directional ≥ 25% of completers / weak < 0.3/user/week.
- **Broadcast-claim SUPPLY rate (promoted):** claims by non-1:1-invited humans (`claimed_via_broadcast`) — read as a SUPPLY signal, not just a ceiling count. Even ONE is a falsifiable k>1 datapoint; bounded by the per-code edge-cap (Critic A-4).
- **In-app finish-card engagement** (partner finished → recipient opens the card): directional ≥ 30% / weak < 15% (an in-app open rate, not a delivery rate).
- **In-app live-async surface + `started_via` conversion — PURE DIAGNOSTIC.** Reported with its render-on-open denominator + the ignition-seen-not-acted counter.
- **Health + SILENT-FRICTION + FELT-COST guards:** mute rate (TWO candidate causes — spam vs ambient-pressure — disambiguated by the optional mute-reason), REMOVE-vs-BLOCK split, bulk-revoke rate, report rate, `partner_dormant` rate (and the inviter-resend rate), Skip-then-return / want-a-partner-intent rate, offline-claim-retry rate (Critic A-2), re-pair rate, distinct-actor failed-claim rate, notification-off rate, ping-consent grant + asymmetry, F-sentiment "rough" rate, the ignition-seen-not-acted counter.

Low Axis A *with reliability + low mute/block/report + healthy consent + acceptable sentiment + low `partner_dormant` + a HEALTHY ask→install recruiting funnel confirmed* = **supply/loop problem → "build the Room next" and/or the W9 join loop**, NOT "the pivot failed." NAMED HONESTLY: the "build the Room" branch rests on §9 conviction + the broadcast-card k>1 count — W7 gathers ZERO live-Room/liquidity datapoint by deliberate §9 choice (openFlaws). If the recruiting funnel shows few asks → it's a recruiting miss, not a loop failure (Critic A-5).

### Axis B — PER-PAIR RETENTION LIFT — DEFERRED TO W9
NOT a W7 deliverable. The randomized control is operationally impossible at this n/team (A7). The `pair_context` tag is logged and BANKED. The "paired ≤ solo" revert trigger DELETED for W7. Qualitative color observed, NOT read as a causal verdict. The visible pair streak is dark (F5). NAMED RISK (openFlaws): W7/W8 ship pairing-first WITHOUT yet checking whether paired users churn FASTER than solo (Risk 2); this coupling-blindness is the deliberate cost of deferring an unrunnable RCT, and W9's first job is the causal read before more is built on the pairing assumption.

### Decision matrix (pre-committed; decider = Mohamed, per L18) — KEEP THE TERMINALS VERBATIM
- **Loop-readable threshold NOT cleared →** the Axis-A read is anecdote. Do NOT launder into "build the Room"; do NOT call pass OR kill. Route: SLIP + re-recruit. A Phase-B/Room bet may NOT be justified by anecdote. (NEW: first consult the recruiting funnel — a low ask count is a recruiting miss, not a loop failure — Critic A-5.)
- **High Axis A (loop fires + compounds) →** the Phase-A loop machinery WORKS; validates shipping Phase A (§9). Plan the W9 causal retention read (Axis B) on the larger cohort — do NOT leap to a Phase-B/marketplace gate on a friend-pairing loop-fire number alone (liquidity/stranger/async-strength are UNINSTRUMENTED).
- **Low Axis A →** loop weak; build the Room and/or the W9 join loop, don't pivot — on §9 conviction + the broadcast-card k>1 count (NAMED as conviction, not Room evidence).
- **started_via conversion →** DIAGNOSTIC ONLY.
- **broadcast-card k>1 / supply count →** directional input to the "build the Room" branch AND a supply read; zero claims is NOT a kill.
- **Supply/threshold miss →** SLIP the beta, NOT revert.
- **Ambiguous →** inconclusive != kill; first check A0 + rules-deploy + the two release-gate predicates + old-client×new-rules + existing-cohort migration + the sacred-middle invariant (incl. resume) + the daily-reminder-survives bar + the loop-readable threshold + the recruiting funnel + the correctness bars; if any failed or recruitment is thin, fix/recruit-more/re-read/slip — do NOT revert on an artifact or a supply miss.

**Clean read:** Axis A = *did the loop machinery fire* + *does it compound at all* (the W7 deliverable, made likely by Day-0 recruiting); Axis B = *did pairing retain causally* (DEFERRED to W9). De-risked structurally: A0 reconciled, the two release-gate rule predicates proven under-cap, the existing-cohort migration + old-client window verified, the sacred-middle invariant true across resume, the daily reminder surviving a session, rules-deployed-before-OTA, the pairs RECRUITED FROM DAY 0 with a tallied funnel, a write-idempotent live meter, land-paired reliability on a clean denominator, a non-coercive AND non-empty AND non-permanently-lying AND non-residual-beacon AND pressure-acknowledged consent stream, ONE consolidated consent, the inviter kept oriented through a dormant partner with a non-blank name, no title leak AND no free-text/stale/poison name leak, an in-app finish card, the wipe-completeness floor incl. the cross-tree grant, a bounded coarse-only broadcast SUPPLY edge, a discoverable report path, ONE k>1 probe, a decision matrix with a reachable "thin → slip, do not launder" terminal, and NOT staking any build/pivot on a crippled conversion number or a causal retention n this team cannot validly collect this week.

---

## 8. Day-by-day sequencing (~1 week, 2 people, NO native build, ~5-6 effective M-days after the relocation buffer, RECRUITING FROM DAY 0)

**Day 0 (joint, FULL day) — recruiting STARTS today:** FIRST the channel-match check (LIVE binary appVersion + channel match the Day-7 target). THEN reconcile A0 WITH the relocation buffer. Lock A7 (Axis B deferred). Lock A1-A8, A4b-A4g. Write F-recruit: the named list + M as owner + the ask→yes→sent→installed tally + the loop-readable threshold + the slip tripwire (NO arm). M BEGINS RECRUITING TODAY against stubs. M PRE-WRITES the additive `firestore.rules` (Egypt-SPOF mitigation) — including the two release-gate predicates and the cross-tree unpair grant. Agree all contracts (the `analytics_events` shape + idempotency key; `cancelEntrySessionReminders()` AS A GENERALIZATION of the existing break-cancel helper + its TWO EXISTING call sites — NOT a focus.tsx wiring; the IN-APP finish-card read contract; the wipe-enumeration helper incl. the parallel single-doc deleter + name + pointer + intent; the DECOUPLED name projection writer signature; the staleness-bounded ignition render incl. sign-out/just_finished decay; the `social/summary` + `social/profile` + `pair_context` shapes; presence/sentiment/report/intent shapes; the invite constants + the singleton-pointer guard + the create-rule get()-budget + the broadcast multi-claim+edge-cap + the offline-claim state — NO issuer-ack but capped notify outside the transaction + bulk-revoke; the SINGLE `share_consent` flag; `started_via`/`pair_context`/`claimed_via_broadcast`). Room density + SNAPSHOT get()-cost DEFERRED. F-coflog writer CUT. M publishes all stubs. Sign off before feature code.

**Day 1 (M + S — the notif REUSE is cheap; native pre-check; rules + the two release-gate predicate tests begin; M recruits):** M7.notif-fix (CHEAP): add `cancelEntrySessionReminders()` + reschedule-on-end + the both-branches + resume + audit-#30-not-reintroduced tests. NATIVE PRE-CHECK: verify on the ACTUAL installed TestFlight binary that `getAllScheduledNotificationsAsync` returns the DAILY trigger shape — Day 1. Begin M7.0a `firestore.rules` + `analytics_events` sink — **and FIRST the two RELEASE-GATE predicate tests (Critic B-1/B-2): the `acceptInvite` CREATE-rule get()-count under the Spark 10-cap, and the sorted-UID `isPartner()` partner-read — before any UI builds on them.** M CONTINUES RECRUITING + tallies the funnel. S: S7.0 pairing + F1-states UI (inviter-oriented dormant; the OFFLINE/Retry state; no issuer-ack) + the no-friend "Skip" + want-a-partner intent; the SINGLE pairing-consent (default OFF); the REPORT surface; S7.notif-swap (swap the two existing `cancelBreakReminder()` calls); wire `logEvent` + the MMKV ignition counter.

**Day 2 (M + S — PROTECTED co-presence FRONT-LOADED; the Days-2-3 backend stack is the dominant risk, DEFERRABLE absorbs slip; M recruits):** M7.3 begins EARLY: presence WRITE (with `started_at` + `ended_at`, sign-out idle) + staleness-bounded LIVE single-partner-read + listener-mount/teardown + the IN-APP finish-card read path. M7.2 title-stripping `social/summary` + the DECOUPLED name-projection writer (pair-accept + edit + content-check) + `pair_context`. M7.1 `acceptInvite` + first-class broadcast funnel (multi-claim + edge-cap) + capped notify outside the transaction + bulk-revoke + F1-states (incl. offline) + `floq://`. Continue M7.0a rules (incl. the cross-tree unpair grant) + M7.0 partner edge + singleton pointer + mute + REMOVE/BLOCK + read-rule revocation + teardown. Notif fix verified on the EXISTING binary. M CONTINUES RECRUITING. S: S7.1 card-as-invite + the Partner-tab invite home + S7.4 reward shell + F-sentiment; S7.route deep-link nav.

**Day 3 (M + S — PRIORITY-A + PROTECTED co-presence essentially complete; the test pins):** Close M7.0a rules + the two release-gate predicate tests + the cross-tree unpair grant test + M7.lifecycle (wipe-completeness via the parallel single-doc deleter incl. NAME + pointer + intent + emulator test; NO co_sessions) + M7.2 + M7.0 tests + the singleton-transaction emulator test S can run + M7.3 cancel-on-entry integration + handler closure + the STALE/sign-out/just_finished suppression checks. M CONTINUES RECRUITING. S: S7.2 Beat-1 staleness-bounded live surface + teardown + `started_via` + ignition-counter; S7.3 Beat-2 true-at-start chip.

**Day 4 (M recruiting CONTINUES + verification; S):** M: recruiting continues to loop-readability (the back-half of a Day-0-started effort); verification + hardening + the old-client × new-rules window check (Critic A-6/B-5). S: S7.4 reward completion (in-app finish card F4-inapp, in-app rate-capped context-anchored fire/clap F6 — self/double/mutual enumerated) + S7.perm (SECOND-session-end / first-reaction). F-sentiment gate-checked.

**Day 5 (M + S) — F-recruit LOOP-READABLE TRIPWIRE CHECK (NO powered floor, NO arm):** Count live pairs against the loop-readable threshold AND consult the ask→yes→sent→installed tally (a low ask count is a recruiting miss, not a loop failure — Critic A-5); if so thin the loop metrics are anecdote, **SLIP THE BETA A WEEK and keep recruiting (KEEP VERBATIM)** — the last resort, made unlikely by Day-0 recruiting. Freed slot → F1 multi-device + conversion + enumeration + offline-retry hardening, the `social/summary` no-title + name-projection content-check verification, consent/first-touch checks. Instrumentation completeness: all Axis-A funnel events + the ignition counter + the `claimed_via_broadcast` supply tag route through F0 with the idempotency key.

**Day 6 (S; M review) — gate SPLIT INTO TWO PASSES:** **PASS 1 (privacy/erase floor + the release-gate predicates — the never-cut bar, run FIRST):** the emulator rules + wipe tests — partner DENIED on `sessions/{id}` + `social/<other>` + sentiment + report + the owner doc; **the sorted-UID `isPartner()` partner-read grants/denies correctly (Critic B-1);** **the `acceptInvite` CREATE-rule get()-count is under the Spark 10-cap (Critic B-2);** wipe leaves a user unreadable across `social/summary` + NAME projection + sentiment + report + `presence` + the singleton pointer + the want-a-partner intent; **the cross-tree unpair grant is member-only (Critic B-3);** the singleton pointer rejects a second partner; a removed/blocked SAME-UID partner cannot re-pair; a dead-issuer claim resolves cleanly. Scriptable emulator tests. **PASS 2 (end-to-end felt-experience, two clean devices + an ALREADY-ONBOARDED binary with a pre-existing scheduled daily reminder):** invite → install (or no-friend Skip → want-a-partner intent; or offline → Retry) → land paired IMMEDIATELY (issuer NOTIFIED capped) → co-presence → IN-APP finish card → in-app reaction → card → sampled sentiment. Verify hard gates: funnel events land (idempotent); no notification during a session — cancel-on-entry INCLUDING the daily reminder within a minute of the preferred hour AND on a RESUMED session; the daily reminder still exists with the right hour after a session across both permission branches; a crashed/stale `focusing`, a signed-out partner, and a stale `just_finished` do NOT render "focusing now"/"just finished" (Critic A-3); a `partner_dormant` partner shows the INVITER the oriented "{name} joined" state + resend with a NON-BLANK name (Critic A-1); pairing is ONE consent decision; mute silences + tears down the listener; REMOVE one-tap / BLOCK confirm + read-rule revocation; OS permission at SECOND session-end / first-reaction; a broadcast-card claim opens only a COARSE-ONLY edge bounded by the per-code edge-cap + notifies the issuer + is bulk-revocable; the REPORT path works. `pnpm typecheck` + `pnpm test` green; no cold-boot console errors. The two passes are separable so PASS 1 survives if PASS 2 compresses.

**Day 7 (buffer / W8 handoff):** FIRST deploy `firestore.rules` + verify (+ the old-client × new-rules window check), THEN ship the W7 surface as an OTA `eas update`; confirm the existing-cohort migration resync runs on first-open. Confirm dashboards. Ship to the RECRUITED pairs — ONLY if the loop-readable threshold cleared; else the beta is the slipped week's target. If buffer remains: the DEFERRED W9-prep (F7 N-member, SNAPSHOT get()-count, Room density doc, the W9 k-anon aggregate-survivor design, the W9 Axis-B causal-retention design, the timely-reaction push) + S7.6 pair-insight UI.

**Critical path:** channel check + native-behavior pre-check + A0 + A7 + A1-A8/A4g + F-recruit named list + funnel tally + M BEGINS RECRUITING DAY 0 → M7.notif-fix generalize-existing-helper + S swaps the two existing call sites + both-branches test (CHEAP) → F0 sink (idempotent) + M7.0a scoped rules (pre-written Day 0; Days 1-3) + **the two RELEASE-GATE predicate tests (Critic B-1/B-2)** + the singleton guard + the cross-tree unpair grant → M7.1 code claim + first-class broadcast funnel + offline state → M7.2 title-stripping + the DECOUPLED content-checked name-projection + `pair_context` → M7.3 staleness-bounded presence (incl. sign-out/just_finished decay) + IN-APP finish card + F3 (Days 2-3) → M7.lifecycle wipe-completeness (parallel single-doc deleter) → S7.0/S7.1/S7.2/S7.4 → rules-deploy-then-OTA + migration + old-client-window check → the RECRUITED-TO-LOOP-READABILITY pairs. DEFERRABLE slips first.

**Fallbacks (if anything slips):** drop order is S7.6 pair-insight UI → the Room density doc → the SNAPSHOT get()-count assertion → F7 N-member proof. **Never** cut: F-recruit-worked-from-Day-0 + its funnel tally (the numerator), the notif cancel-on-entry generalization, the F0 sink (idempotent), F-lifecycle wipe-completeness (incl. the parallel single-doc deleter + NAME + pointer + intent + the cross-tree unpair grant), the F1 code claim + F1-states (incl. the inviter-oriented dormant state with a non-blank name + the offline state) + no-friend pass-through + entropy + the singleton guard + **the two RELEASE-GATE rule predicate tests (Critic B-1/B-2)**, the EXACT-path privacy boundary + the DECOUPLED content-checked name projection + owner-doc DENY, the single consolidated consent + mute/REMOVE/BLOCK/report + read-rule revocation + listener teardown, the presence staleness bound (incl. sign-out/just_finished decay), the F-sentiment + `pair_context` tag, the loop-readable threshold + the matrix's "thin → slip" terminal (KEEP VERBATIM), the non-synchronous + non-empty + non-permanently-lying + pressure-acknowledged ignition language, the rules-deploy-before-OTA order + migration + old-client-window check, the §7 correctness bars, the Day-6 PASS-1 privacy/erase/release-gate emulator suite. The visible streak, the Room UI, and the Axis-B causal verdict stay dark/deferred. The W8 Axis-A read does NOT slip on an engineering miss — but it MAY slip a WEEK on a SUPPLY miss, which Day-0 recruiting is designed to PREVENT, not merely detect.

---

**Files for execution:** `/Users/mohamedhiba/Projects/floq/shared/spec/decisions.md` (amend A0-A8 + A4b/c/d/e/f/g; L11 line 84; A7 Axis-B-deferred; L13/L23 lock Spark), `/Users/mohamedhiba/Projects/floq/backend/firestore.rules` (M7.0a — `TODO(M7.1)` hook at line 20; the `{document=**}` owner-grant at lines 24-26 the EXACT-PATH partner-read must NOT extend AND the partner DENIED on the raw owner doc; the OR-combination danger — Critic B-1; `training_samples` `hasOnly` create-only/`read:false` precedent at line 36; add the EXACT-path `social/summary` + `social/profile` name-projection partner-reads via the sorted-UID `isPartner()` predicate — the riskiest rule, test-pinned; the `acceptInvite` CREATE-rule with its get()-count proven under the Spark 10-cap — release gate, NOT W9; the singleton-pointer write guard; the NARROW member-only cross-tree unpair write-grant — Critic B-3; the broadcast per-code edge-cap; the F-lifecycle owner DELETE rules incl. name projection + the `users/{uid}/partner` pointer + the want-a-partner intent doc; NO `co_sessions` write rule), `/Users/mohamedhiba/Projects/floq/mobile/components/session/useStartSession.ts` (VERIFIED `launch()` at line 43 ALREADY calls `void cancelBreakReminder()` — generalize to `cancelEntrySessionReminders()`; S's lane), `/Users/mohamedhiba/Projects/floq/mobile/components/session/RestoreSessionPrompt.tsx` (VERIFIED `onResume()` at line 52 ALREADY calls `void cancelBreakReminder()` with a "symmetric with useStartSession.launch" comment — generalize it; S's lane), `/Users/mohamedhiba/Projects/floq/mobile/app/focus.tsx` (VERIFIED lines 177-196 are a HYDRATION SEAM, not a session-start site — the cancel does NOT belong here), `/Users/mohamedhiba/Projects/floq/mobile/services/notifications/index.ts` (VERIFIED `cancelByKind` + the `breakScheduleInFlight` audit-#30 guard + `cancelBreakReminder` are the pattern `cancelEntrySessionReminders()` GENERALIZES to also sweep `session-start`; `scheduleSessionStartReminder` needs `PreferredTime` + `sessionStartReminderEnabled` + permission threaded, and `{request:false}` must not permanently delete the reminder), `/Users/mohamedhiba/Projects/floq/mobile/stores/useActiveSessionStore.ts` (`startSession` is sync — the cancel does NOT live here; it lives at the two component start sites), `/Users/mohamedhiba/Projects/floq/mobile/app/index.tsx` (line 101 reschedules the daily reminder with `{request:false}` on launch — the launch-resync + existing-cohort migration mirror; also hydrates the absent `partner` pointer as null for the existing cohort), `/Users/mohamedhiba/Projects/floq/mobile/services/session/restore.ts` (sessions survive app-kill — the F3 presence-staleness `started_at` source), `/Users/mohamedhiba/Projects/floq/mobile/CLAUDE.md` (VERIFIED line 102 "Invite pending → Partner tab shows 'waiting on {name}' with a resend/cancel affordance" — the inviter-oriented dormant state restores it; the route is ALREADY `partner.tsx` so no rename — Critic B route-point moot), `/Users/mohamedhiba/Projects/floq/mobile/app/(tabs)/partner.tsx` (VERIFIED the partner tab route already exists — no `friends.tsx` rename in the OTA), `/Users/mohamedhiba/Projects/floq/mobile/package.json` (VERIFIED no analytics package — F0 hand-rolled), `/Users/mohamedhiba/Projects/floq/mobile/services/session/distraction.ts` (line 42 `title: s.task.title` — the VERIFIED leak the `social/summary` projection must NOT mirror), `/Users/mohamedhiba/Projects/floq/mobile/services/session/finalize.ts` (M7.2's title-stripping + `pair_context` projection hook + the name-projection REFRESH (not source); the reschedule-on-end call site; the IN-APP finish-card read derives from synced summaries — NO digest scheduler), `/Users/mohamedhiba/Projects/floq/mobile/services/firebase/userDoc.ts` (VERIFIED `display_name` user-typed free text at line 18 — the name a partner sees must come from a projection, in the wipe enumeration), `/Users/mohamedhiba/Projects/floq/mobile/services/firebase/userProfile.ts` (VERIFIED `updateDisplayName` at line 64 writes `display_name` at ANY time — Critic A-1: the name projection MUST fire here + on pair-accept, with a content check, NOT only on finalize), `/Users/mohamedhiba/Projects/floq/mobile/services/firebase/userData.ts` (VERIFIED `wipeRemoteUserData` lines 64-73 deletes ONLY `sessions`+`tasks` via `deleteSubcollection` typed `'sessions'|'tasks'` at line 47 — Critic B-3: F-lifecycle needs a PARALLEL single-doc deleter for the 7 new single-doc egress + the cross-tree unpair write; NO co_sessions, NO aggregate survivor), `/Users/mohamedhiba/Projects/floq/mobile/services/sync/` (the tombstone pattern F-lifecycle reuses), `/Users/mohamedhiba/Projects/floq/mobile/ios/floq/floq.entitlements` (VERIFIED `aps-environment` line 5 ALREADY PRESENT — no rebuild), `/Users/mohamedhiba/Projects/floq/mobile/app.json` (VERIFIED `runtimeVersion.policy=appVersion` line 62, `floq://` scheme; UNCHANGED — Critic B-6: `floq://` cannot open an uninstalled app, so cold-install pairing is 100% typed code), `/Users/mohamedhiba/Projects/floq/mobile/eas.json` (VERIFIED channels — Day-0 channel-match targets `production`; A4g rules-deploy-before-OTA + migration + the old-client × new-rules window check), `/Users/mohamedhiba/Projects/floq/shared/spec/tasks.md` (M7.0/M7.1b/S7.0/S7.1b split; F9 drop supersedes M7.1b at line 843), `/Users/mohamedhiba/Projects/floq/docs/strategy-social-as-core.md` (§4 line 73 + §9 line 145 — the basis for REJECTING the live-Room demand and DEFERRING Axis B; the F-coflog cut's NAMED §9 moat cost), `/Users/mohamedhiba/Projects/floq/docs/` (the Room density-orchestration spec DEFERRED + the F-recruit named-pairs list + the ask→yes→sent→installed funnel tally + the A7 Axis-B-deferral record + the W9 Axis-B causal-retention design + the want-a-partner-intent W9 Room-seed note).